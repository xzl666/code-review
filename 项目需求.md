现在需要你设计一个 **AI 驱动的代码检视平台** 的整体技术方案。
请严格依据下述需求和约束，输出一份结构清晰、可直接落地的详细设计文档。

## 需求背景
我们要做一个代码检视系统，核心是用claude + 自定义的大模型api）对多个代码仓库进行自动化审查 + 常见问题执行脚本

### 功能要求
1. **项目管理**  
   - 支持多个项目，每个项目关联一个 Git 代码仓库（Gitee）。  
   - 可配置仓库地址、访问令牌、默认分支等、责任人、可通过excel导入项目信息

2. **检视规则配置**  
   - 允许用户自定义检视规则，包括：规则名称、类型（安全性、性能、代码风格、自定义）、AI 提示词模板、严重等级。  
   - 允许自定义脚本，也可以通过AI生成脚本
   - 每条规则可关联一个“Skill”（即大模型的 Function Calling 定义），以便 AI 输出结构化的问题数据。

3. **AI 检视逻辑（核心）**  
   - 接入deepseek大模型，通过兼容 OpenAI 的接口调用，不更换 API Key，集中管理密钥。  
   - 让模型以标准 JSON 格式输出：问题位置、严重程度、摘要、修改建议等。  
   - 检视范围：可配置“审查最近 N 天内的提交”，系统自动拉取这些提交的代码 diff，结合规则 Prompt 发送给大模型 + 执行自定义的脚本规则

4. **触发方式**  
   - **定时触发**：每天固定时间（如凌晨）自动执行一次全量检视。  
   - **手动触发**：支持在界面上对某个项目一键发起检视。  

5. **检视结果与通知**  
   - 每次检视生成一个任务记录，保存发现的所有问题（文件、行号、严重度、AI 建议等）。  
   - 通知渠道可配置：实现接口，自定义实现通知。通知消息内容模板可编辑。  
   - 检视完成后自动推送总结报告（问题数量、严重度分布等）。

6. **前端展示**  
   - 现代化管理后台，界面美观  
   - 包含：项目列表、规则配置、检视任务历史、问题详情、趋势图表等。

### 技术约束
- **后端**：JDK 8， Spring Boot 2.7， MyBatis-Plus， MySQL 5.7。  
- **AI 模型**：deepseek，调用方式为 HTTP REST，兼容 OpenAI chat/completions 格式。必须使用 Function Calling 实现结构化输出。  
- **安全**：API Key 不能硬编码，需加密存储。  
- **性能**：拉取代码和 AI 调用需异步处理，避免阻塞主线程。  

## 输出要求
请按以下顺序输出详细的 Markdown 设计文档，内容需包含但不限于：

1. **系统整体架构图**（用文字描述各组件及其交互）  
2. **后端模块划分与职责**（如项目模块、规则模块、检视引擎、通知模块等）  
3. **数据库核心表结构**（列出字段名、类型、注释，标明关系，数据库严禁使用外键，需要有固定字段create_time,update_time,deleted）  
4. **AI 检视引擎详细设计**  
   - Prompt 组装策略（如何将规则模板、代码 diff、Skill 描述组合）  
   - Function Calling 的 JSON Schema 定义（给出一个示例 Skill 的完整定义）  
   - 结果解析与持久化流程  
5. **定时调度与防重机制**（技术选型，如何使用分布式锁）  
6. **消息推送设计**（渠道抽象、模板渲染、触发时机）  
7. **关键  API 列表**（URL、方法、用途、所有的api都是post请求，要求遵循ddd四层架构）  
8. **前端页面结构**（主要页面和功能模块）  

## 附加指示
- 整个设计必须突出“AI Skill”在检视规则中的应用。
- 方案需考虑生产环境的可扩展性和安全性。