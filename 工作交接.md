# 工作交接文档

更新时间：2026-05-19

本文用于在更换模型或重新开启会话时快速恢复项目上下文。接手时建议先读本文，再读 `总体设计方案.md`、`剩余工作.md` 和最近一次 `git log`。

## 项目定位

本项目是一个 AI 驱动的代码检视平台，当前按 MVP 闭环推进。核心能力包括：

- 项目管理：维护被检视仓库、默认分支、检视天数、定时检视 Cron。
- 检视任务：支持手动触发、定时触发、任务状态流转、任务统计、异常信息记录。
- 检视引擎：拉取仓库 diff，按 commit/file/hunk 拆分 chunk，执行 AI 规则和脚本规则，落库问题。
- AI Skill + 规则管理：维护 AI function schema、Prompt、规则严重度、规则启停。
- 脚本规则：维护 Shell/Python/Node 脚本，支持测试运行和结果解析。
- 问题闭环：问题列表、详情、忽略、标记修复、导出、起止行、检查时间、修复建议。
- 通知：Webhook 配置、通知模板、任务完成/失败通知。
- 首页看板：统计项目、任务、问题趋势、严重度分布。

## 技术栈与运行环境

- 后端：Spring Boot 2.7.18、JDK 17、MyBatis-Plus、MySQL。
- 前端：Vue 3、Vite、TypeScript、Element Plus、ECharts、lucide-vue-next。
- 数据库：MySQL 5.7，部署在云服务器 `43.163.119.246:33066`，库名 `code_review`。
- 本地后端端口：`8080`。
- 本地前端端口：常用 `5174`，Vite 代理 `/api` 到 `http://localhost:8080`。
- 代码仓库：当前本地目录 `G:\javastudy\code2\code-review`。
- JDK：已切换为 JDK 17，`pom.xml` 中 `java.version=17`。

敏感配置不要写死到代码中：

- `DEEPSEEK_API_KEY`：DeepSeek API Key，由用户本地手动写入。
- `GITEE_TOKEN`：Gitee Token，由用户本地手动写入。
- `CODE_REVIEW_DB_HOST`、`CODE_REVIEW_DB_PORT`、`CODE_REVIEW_DB_NAME`、`CODE_REVIEW_DB_USERNAME`、`CODE_REVIEW_DB_PASSWORD`：数据库连接配置，有默认值指向云 MySQL。

DeepSeek 当前配置口径：

- baseUrl：`https://zhenze-huhehaote.cmecloud.cn`
- model：`deepseek-v4-flash`
- API Key：从本地环境变量读取

## 常用命令

后端编译：

```powershell
mvn -q -DskipTests compile
```

后端打包：

```powershell
mvn -q -DskipTests package
```

前端构建：

```powershell
cd frontend
npm run build
```

启动前端开发服务：

```powershell
cd frontend
npm run dev -- --port 5174
```

启动后端 jar：

```powershell
java -jar target\code-review-0.0.1-SNAPSHOT.jar
```

验证首页看板接口：

```powershell
Invoke-RestMethod -Method Post -Uri http://localhost:8080/api/dashboard/overview -ContentType 'application/json' -Body '{}'
Invoke-RestMethod -Method Post -Uri http://localhost:8080/api/dashboard/issue-trend -ContentType 'application/json' -Body '{"days":7}'
Invoke-RestMethod -Method Post -Uri http://localhost:8080/api/dashboard/severity-distribution -ContentType 'application/json' -Body '{}'
```

## 当前进度

已完成：

- 根据总体设计完成任务拆分，拆分记录见 `任务拆分.md`。
- 将中间件和部署信息写入总体设计。
- API 命名已按短横线形式推进，避免小驼峰路径。
- MySQL 5.7 已部署到云服务器 `43.163.119.246:33066`。
- 后端基础模块、项目管理、规则管理、AI Skill、脚本规则、任务执行链路、问题闭环、通知配置已经形成 MVP。
- 前端项目已搭建，并完成主要业务页面。
- 项目管理已增加定时检视 Cron 配置和手动触发一次检视按钮。
- 问题列表已增加“检视任务编号”输入框，支持输入任务 ID 或任务编号筛选。
- Element Plus 分页英文已本地化为中文。
- 问题详情已补充检查时间、起始行、结束行。
- AI/脚本问题解析已兼容 `summary/title/message/description`、`startLine/line/newLine`、`endLine/lineEnd/newEndLine`、`suggestion/suggestedFix/fix/recommendation` 等字段。
- AI Prompt 已要求 `summary/detail/suggestion` 等描述性字段使用中文。

- 首页看板原来是静态假数据，现已改为调用真实 dashboard 接口，并完成前端页面验证。
- 后端 `DashboardOverviewResponse` 已补充：
  - `openIssueCount`
  - `todayAiCallCount`
- 后端 `/api/dashboard/issue-trend` 已支持传入 `days`，当前前端支持 7 天 / 30 天切换。
- 前端新增 `frontend/src/api/dashboard.ts`，`DashboardView.vue` 已接入 overview、trend、severity 接口。
- 启动迁移服务 `SchemaMigrationService` 已补齐 `error_message` 列自动迁移，并让 `ReviewTaskAppService` 依赖迁移先执行。
- `pom.xml` 已补充 UTF-8 编译编码配置。
- 已补充 `DashboardAppServiceTest`，覆盖 overview 统计、严重度中文标签和趋势天数边界。
- 已抽取 `ReviewIssuePayloadParser`，AI 和脚本问题结果共用一套字段兼容解析逻辑。
- 已补充 `ReviewIssuePayloadParserTest`，覆盖摘要、行号、修复建议、文件路径等字段别名。
- 已补充 `DeepSeekClientTest`，覆盖 Prompt 中的中文描述字段和起止行要求。
- 已增加通知投递日志实体、Mapper、初始化建表和启动迁移。
- 已增加通知失败后最多 3 次的定时重试机制。
- 已增加通知投递日志分页接口 `/api/notify-delivery-log/page`。
- 已在前端通知配置页增加“投递日志”Tab，支持按任务编号、事件类型、状态筛选。
- 前端路由已改为懒加载，Vite 已配置 `manualChunks` 拆分 Vue、Element Plus、ECharts、图标库。
- 首页 ECharts 已改为按需引入 line/grid/tooltip/canvas，Element Plus 已改为子模块导入并按当前使用组件注册。
- 脚本执行已抽取统一 `ScriptSandboxExecutor`，脚本测试和检视任务执行共用进程级沙箱：临时目录、环境白名单、危险命令拦截、输入/输出限制、超时强杀和执行后清理。

## 最近一次关键验证

已通过：

- `mvn -q -DskipTests compile`
- `npm run build`
- 首页 overview 接口曾返回真实数据，例如：
  - `projectCount=1`
  - `enabledProjectCount=1`
  - `todayTaskCount=9`
  - `todayIssueCount=8`
  - `openIssueCount=10`
  - `todayAiCallCount=9`

已继续验证：

- 2026-05-19 已重启最新后端 jar，`/api/dashboard/overview` 返回真实数据。
- 2026-05-19 使用 UTF-8 原始响应确认 `/api/dashboard/severity-distribution` 返回“阻断、严重、主要、次要、提示”。PowerShell 控制台可能显示乱码，但浏览器和 UTF-8 解码正常。
- 2026-05-19 已打开 `http://127.0.0.1:5174/dashboard`，确认四个指标卡、趋势图、严重度分布正常渲染。
- 2026-05-19 已通过 `mvn -q test` 和 `npm run build`。
- 2026-05-19 已补充并通过 dashboard、AI 解析、DeepSeek Prompt 相关单元测试。
- 2026-05-19 已验证通知投递日志分页接口返回成功。
- 2026-05-19 已打开 `http://127.0.0.1:5174/notifications`，确认“投递日志”Tab 正常显示。

## 重要坑点

1. 后端 jar 被 Java 进程占用时，`mvn package` 会失败，错误类似：

```text
Unable to rename ... code-review-0.0.1-SNAPSHOT.jar to ... jar.original
```

处理方式：先找出占用 `8080` 或 `java.exe` 的进程，确认是当前项目后再停止。

2. 远端 MySQL 第一次连接偶发较慢或被关闭，日志可能出现：

```text
No operations allowed after connection closed
```

通常重启后端可恢复。端口连通性可用：

```powershell
Test-NetConnection -ComputerName 43.163.119.246 -Port 33066
```

3. 数据库结构可能滞后于代码。项目已有 `SchemaMigrationService` 启动时补列，目前至少会补：

- `cr_project.schedule_cron`
- `cr_project.schedule_enabled`
- `cr_review_task.skipped_commit_count`
- `cr_review_task.skipped_file_count`
- `cr_review_task.warning_message`
- `cr_review_task.error_message`
- `cr_notify_delivery_log` 表

4. Windows 下 Java 源码中文曾出现编译后乱码，所以 `pom.xml` 已加入 UTF-8 编码配置。若接口仍乱码，可优先检查是否启动的是旧 jar。

5. 前端已配置路由懒加载和基础 vendor 拆包。若后续仍出现 chunk 体积警告，可继续拆分业务公共组件或改造 Element Plus 按需导入。

6. 浏览器自动化插件在一次会话中出现过 `browser-client.mjs` 缺失，导致无法强制打开/控制 in-app browser。若复现，先用本地 URL 手动访问，再继续代码验证。

## Git 记录

最近已提交的重要提交：

- `4ae468f fix: improve issue detail fields`
- `065dfe0 fix: remove issue task id filter`
- `4b73466 fix: localize pagination labels`
- `1d538c3 feat: add project scheduled reviews`
- `f1c730a feat: persist review task warnings`
- `12dca7c feat: complete task issue workflow`
- `abeadae feat: wire remaining admin frontend pages`
- `61e9665 feat: complete backend closeout and wire frontend pages`

用户要求：每次修改都需要提交代码。完成任何修改后执行构建验证，再 `git add` + `git commit`。

## 下一步建议

优先级 1：

- 脚本规则执行已完成进程级沙箱 MVP，下一步是容器级隔离和资源配额。
- 前端继续做 Element Plus CSS 按需加载、页面骨架屏和首屏性能观察。

## 接手检查清单

1. 执行 `git status --short`，确认是否有未提交改动。
2. 执行 `mvn -q -DskipTests compile`。
3. 执行 `cd frontend; npm run build`。
4. 确认后端 `8080` 和前端 `5174` 是否运行。
5. 访问 `http://127.0.0.1:5174/dashboard` 和 `http://127.0.0.1:5174/issues`。
6. 若 dashboard 数据不变，优先检查前端是否请求 `/api/dashboard/*`，再检查后端是否启动了最新 jar。
