# AI 驱动的代码检视平台总体设计方案

## 1. 系统整体架构图

### 1.1 架构目标

本系统用于对多个 Gitee 代码仓库进行自动化代码检视，核心能力包括：

- 多项目 Git 仓库管理
- 自定义 AI 检视规则
- 自定义脚本规则
- AI Skill / Function Calling 结构化问题输出
- 按最近 N 天提交 diff 自动检视
- 定时 / 手动触发检视任务
- 检视结果持久化与趋势统计
- Webhook 通知推送
- 现代化管理后台展示

### 1.2 整体架构文字图

```text
┌──────────────────────────────────────────────┐
│                  前端管理后台                 │
│  项目管理 / 规则配置 / 任务历史 / 问题详情 / 图表 │
└───────────────────────┬──────────────────────┘
                        │ HTTPS / POST API
                        ▼
┌──────────────────────────────────────────────┐
│              Spring Boot 单体后端             │
│                                              │
│ ┌──────────────┐  ┌──────────────┐            │
│ │ 项目管理模块  │  │ 规则配置模块  │            │
│ └──────┬───────┘  └──────┬───────┘            │
│        │                 │                    │
│        ▼                 ▼                    │
│ ┌──────────────────────────────────────────┐ │
│ │              检视任务模块                 │ │
│ │  手动触发 / 定时触发 / 任务状态 / 防重复    │ │
│ └──────────────────┬───────────────────────┘ │
│                    │                         │
│                    ▼                         │
│ ┌──────────────────────────────────────────┐ │
│ │              AI 检视引擎                  │ │
│ │ Git 拉取 diff / Prompt 组装 / Skill 调用   │ │
│ │ Function Calling 解析 / 问题落库           │ │
│ └──────────┬───────────────────┬───────────┘ │
│            │                   │             │
│            ▼                   ▼             │
│ ┌──────────────────┐   ┌──────────────────┐  │
│ │ 脚本规则执行模块  │   │ DeepSeek API 客户端 │  │
│ │ Shell/Python等    │   │ OpenAI兼容接口     │  │
│ └──────────────────┘   └──────────────────┘  │
│                                              │
│ ┌──────────────────────────────────────────┐ │
│ │              通知模块                     │ │
│ │ Webhook 渠道 / 消息模板 / 总结报告推送      │ │
│ └──────────────────────────────────────────┘ │
└───────────────────────┬──────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────┐
│                    MySQL 5.7                 │
│ 项目 / 规则 / Skill / 任务 / 问题 / 通知配置     │
└──────────────────────────────────────────────┘

外部依赖：
┌─────────────┐       ┌─────────────────────┐
│ Gitee 仓库   │       │ DeepSeek Chat API     │
│ Git clone   │       │ OpenAI兼容接口         │
│ Git diff    │       │ Function Calling      │
└─────────────┘       └─────────────────────┘
```

### 1.3 核心调用链路

#### 手动检视链路

```text
用户点击“发起检视”
    ↓
后端创建检视任务
    ↓
任务进入异步线程池
    ↓
根据项目配置拉取 Gitee 仓库
    ↓
计算最近 N 天提交 diff
    ↓
加载启用的 AI 规则和脚本规则
    ↓
对每条规则执行检视
    ↓
AI 规则调用 DeepSeek Function Calling
脚本规则执行本地脚本
    ↓
解析结构化问题
    ↓
保存任务结果和问题明细
    ↓
生成汇总报告
    ↓
通过 Webhook 推送通知
```

#### 定时检视链路

```text
Spring Scheduler 定时触发
    ↓
获取所有启用项目
    ↓
使用分布式锁防止重复执行
    ↓
逐个项目创建检视任务
    ↓
异步执行检视流程
```

---

## 2. 后端模块划分与职责

系统采用 **DDD 四层架构**：

```text
interfaces  接口层
application 应用层
domain      领域层
infrastructure 基础设施层
```

### 2.1 接口层 interfaces

负责接收前端请求、参数校验、DTO 转换、统一响应。

建议包结构：

```text
com.cmbchina.codereview.interfaces
├── controller
│   ├── ProjectController
│   ├── RuleController
│   ├── SkillController
│   ├── ReviewTaskController
│   ├── ReviewIssueController
│   ├── NotifyConfigController
│   └── DashboardController
├── dto
│   ├── request
│   └── response
└── assembler
```

职责：

- 提供 POST API
- 接收项目、规则、任务、通知配置请求
- 参数基础校验
- 调用 application 层服务
- 不包含业务逻辑

---

### 2.2 应用层 application

负责业务用例编排，协调领域对象和基础设施能力。

建议包结构：

```text
com.cmbchina.codereview.application
├── service
│   ├── ProjectAppService
│   ├── RuleAppService
│   ├── SkillAppService
│   ├── ReviewTaskAppService
│   ├── ReviewEngineAppService
│   ├── NotificationAppService
│   └── DashboardAppService
├── command
├── query
└── executor
```

职责：

- 项目创建、编辑、导入
- 检视规则创建、编辑、启停
- Skill 配置管理
- 检视任务创建、调度、状态流转
- AI 检视任务编排
- 通知推送编排
- 趋势统计查询

---

### 2.3 领域层 domain

负责核心业务模型、领域规则和领域服务。

建议包结构：

```text
com.cmbchina.codereview.domain
├── project
│   ├── Project
│   ├── ProjectRepository
│   └── ProjectDomainService
├── rule
│   ├── ReviewRule
│   ├── ReviewSkill
│   ├── ScriptRule
│   ├── ReviewRuleRepository
│   └── RuleDomainService
├── task
│   ├── ReviewTask
│   ├── ReviewIssue
│   ├── ReviewTaskRepository
│   └── ReviewTaskDomainService
├── notification
│   ├── NotifyConfig
│   ├── NotifyTemplate
│   └── NotificationDomainService
└── shared
    ├── enums
    ├── valueobject
    └── exception
```

职责：

- 定义项目、规则、Skill、任务、问题等核心领域模型
- 管理任务状态流转
- 定义规则类型、严重等级、任务状态枚举
- 校验业务不变量
- 定义 Repository 接口

---

### 2.4 基础设施层 infrastructure

负责数据库、外部 API、Git、脚本执行、加密、锁等技术实现。

建议包结构：

```text
com.cmbchina.codereview.infrastructure
├── persistence
│   ├── mapper
│   ├── entity
│   ├── repository
│   └── converter
├── git
│   ├── GiteeRepositoryClient
│   ├── GitDiffService
│   └── LocalRepositoryManager
├── ai
│   ├── DeepSeekClient
│   ├── OpenAiCompatibleChatClient
│   ├── FunctionCallingRequestBuilder
│   └── AiResponseParser
├── script
│   ├── ScriptExecutor
│   └── ScriptSandboxExecutor
├── notification
│   ├── WebhookNotifier
│   └── TemplateRenderer
├── lock
│   └── MysqlDistributedLock
├── crypto
│   └── SecretEncryptor
└── config
```

职责：

- MyBatis-Plus 数据库访问
- Gitee 仓库 clone / pull / diff
- DeepSeek OpenAI 兼容 API 调用
- Function Calling 参数组装
- 脚本安全执行
- Webhook 推送
- MySQL 分布式锁
- API Key 加密存储和解密使用

---

## 3. 后端核心模块设计

### 3.1 项目管理模块

功能：

- 新增项目
- 编辑项目
- 删除项目，逻辑删除
- 启用 / 停用项目
- Excel 导入项目
- 配置仓库地址、访问令牌、默认分支、责任人
- 配置项目类型：前端项目 / 后端项目

核心字段：

- 项目名称
- 项目编码
- 项目类型
- Gitee 仓库地址
- 项目单独访问令牌：明文配置，不加密存储
- 默认访问令牌：系统级统一配置，所有项目默认共用
- 默认访问令牌每天可手动更新，后续支持账号密码自动获取
- 默认分支
- 责任人
- 最近 N 天检视范围
- 启用状态

Excel 导入字段建议：

| 字段 | 是否必填 | 示例 |
|---|---:|---|
| 项目名称 | 是 | 用户中心 |
| 项目类型 | 是 | 后端项目 |
| 仓库地址 | 是 | https://gitee.com/xxx/user-center.git |
| 项目访问令牌 | 否 | project_token_xxx |
| 默认访问令牌 | 否 | default_token_xxx |
| 默认分支 | 否 | master |
| 责任人 | 否 | 张三 |
| 最近检视天数 | 否 | 7 |

导入策略：

- 项目名称 + 仓库地址作为重复判断依据
- 已存在则更新
- 不存在则新增
- 访问令牌按明文保存，不做加密处理
- 导入结果返回成功数、失败数、失败原因

---

### 3.2 规则配置模块

规则分为两类：

1. AI 检视规则
2. 脚本检视规则

#### AI 检视规则

支持配置：

- 规则名称
- 规则类型
  - 安全性
  - 性能
  - 代码风格
  - 自定义
- 严重等级
  - blocker
  - critical
  - major
  - minor
  - info
- AI 提示词模板
- 关联 Skill
- 适用项目类型
- 启用状态

#### 脚本检视规则

支持配置：

- 脚本名称
- 脚本语言
  - shell
  - python
  - node
- 脚本内容
- 脚本参数模板
- 超时时间
- 严重等级
- 是否由 AI 生成
- 启用状态

脚本规则结果也需要输出统一的问题结构，便于和 AI 问题统一展示。

---

### 3.3 Skill 管理模块

Skill 是本系统的核心设计点。

每条 AI 规则可以关联一个 Skill，Skill 本质上是 DeepSeek / OpenAI Function Calling 的函数定义。

Skill 用于约束 AI 输出结构，避免模型返回不可解析的自然语言。

Skill 配置内容：

- Skill 名称
- Function name
- Function description
- JSON Schema
- 版本号
- 启用状态

示例：

```text
规则：SQL 注入风险检查
关联 Skill：report_code_review_issues
作用：要求模型通过 function_call 返回标准化问题数组
```

---

### 3.4 检视任务模块

任务来源：

- 手动触发
- 定时触发

任务状态：

```text
PENDING    待执行
RUNNING    执行中
SUCCESS    成功
FAILED     失败
CANCELED   已取消
```

任务粒度：

- 每次对单个项目发起检视，生成一条任务记录
- 每个任务下有多条问题记录
- 每个任务记录检视范围、提交数量、diff 文件数量、AI 调用次数、耗时等信息

---

### 3.5 AI 检视引擎模块

职责：

- 获取项目仓库
- 计算最近 N 天 diff
- 获取启用规则
- 按规则组装 Prompt
- 构造 Function Calling 请求
- 调用 DeepSeek
- 解析 function_call 结构化结果
- 持久化问题
- 生成统计结果

---

### 3.6 通知模块

第一期先支持 Webhook。

设计为可扩展接口：

```java
public interface Notifier {
    String channelType();

    NotifyResult send(NotifyMessage message);
}
```

第一期实现：

```java
public class WebhookNotifier implements Notifier {
    @Override
    public String channelType() {
        return "WEBHOOK";
    }

    @Override
    public NotifyResult send(NotifyMessage message) {
        // HTTP POST webhook
    }
}
```

后续可扩展：

- 企业微信
- 钉钉
- 邮件
- 飞书
- 自定义 HTTP 推送

---

## 4. 中间件部署信息

### 4.1 MySQL 5.7

当前项目数据库中间件采用 Docker 部署在云服务器上，作为后端服务、任务调度、规则配置、问题落库和通知配置的统一数据存储。

| 配置项 | 配置值 |
|---|---|
| 云服务器地址 | `43.163.119.246` |
| 中间件 | MySQL |
| 版本 | `5.7.44` |
| Docker 镜像 | `mysql:5.7` |
| 容器名称 | `code-review-mysql` |
| 宿主机端口 | `33066` |
| 容器端口 | `3306` |
| 数据目录 | `/data/mysql57-33066/data` |
| 配置目录 | `/data/mysql57-33066/conf` |
| 日志目录 | `/data/mysql57-33066/logs` |
| 数据库账号 | `root` |
| 数据库密码 | `1234` |
| 字符集 | `utf8mb4` |
| 排序规则 | `utf8mb4_unicode_ci` |
| 时区 | `+08:00` |

后端连接配置建议：

```yaml
spring:
  datasource:
    url: jdbc:mysql://43.163.119.246:33066/code_review?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: ${CODE_REVIEW_DB_PASSWORD}
```

运维约定：

- 云服务器防火墙已放开 `33066` 端口。
- MySQL 容器使用 `--restart unless-stopped`，服务器重启后自动恢复。
- `root@%` 已开启，可支持远程连接。
- 生产环境建议将数据库密码放入环境变量或配置中心，不建议在代码仓库中保存明文密码。
- 云服务器 SSH 登录密码属于服务器凭据，不写入项目设计文档。

---

## 5. 数据库核心表结构

约定：

- 数据库：MySQL 5.7
- 不使用数据库外键
- 所有关联通过业务字段维护
- 所有表固定字段：
  - `create_time`
  - `update_time`
  - `deleted`
- 主键建议使用 `BIGINT`
- 金额无关，时间统一使用 `DATETIME`
- 逻辑删除：`deleted = 0` 表示未删除，`deleted = 1` 表示已删除

---

### 5.1 项目表：`cr_project`

| 字段名 | 类型 | 注释 |
|---|---|---|
| id | BIGINT PK | 主键 |
| project_name | VARCHAR(128) | 项目名称 |
| project_code | VARCHAR(64) | 项目编码 |
| project_type | VARCHAR(32) | 项目类型：FRONTEND/BACKEND |
| repo_url | VARCHAR(512) | Gitee 仓库地址 |
| project_token | VARCHAR(1024) | 项目单独访问令牌，明文存储 |
| use_default_token | TINYINT | 是否使用默认访问令牌 |
| default_branch | VARCHAR(128) | 默认分支 |
| owner_name | VARCHAR(64) | 责任人 |
| review_days | INT | 默认检视最近 N 天提交 |
| status | TINYINT | 状态：1 启用，0 停用 |
| remark | VARCHAR(512) | 备注 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| deleted | TINYINT | 逻辑删除 |

索引建议：

```sql
idx_project_code(project_code)
idx_project_type(project_type)
idx_status(status)
```

---

### 5.2 检视规则表：`cr_review_rule`

| 字段名 | 类型 | 注释 |
|---|---|---|
| id | BIGINT PK | 主键 |
| rule_name | VARCHAR(128) | 规则名称 |
| rule_code | VARCHAR(64) | 规则编码 |
| rule_kind | VARCHAR(32) | 规则种类：AI/SCRIPT |
| rule_type | VARCHAR(32) | 规则类型：SECURITY/PERFORMANCE/STYLE/CUSTOM |
| severity | VARCHAR(32) | 默认严重等级 |
| project_type | VARCHAR(32) | 适用项目类型：FRONTEND/BACKEND/ALL |
| prompt_template | TEXT | AI 提示词模板 |
| skill_id | BIGINT | 关联 Skill ID |
| script_id | BIGINT | 关联脚本 ID |
| status | TINYINT | 状态：1 启用，0 停用 |
| sort_order | INT | 排序 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| deleted | TINYINT | 逻辑删除 |

索引建议：

```sql
idx_rule_kind(rule_kind)
idx_rule_type(rule_type)
idx_skill_id(skill_id)
idx_status(status)
```

---

### 5.3 AI Skill 表：`cr_ai_skill`

| 字段名 | 类型 | 注释 |
|---|---|---|
| id | BIGINT PK | 主键 |
| skill_name | VARCHAR(128) | Skill 名称 |
| skill_code | VARCHAR(64) | Skill 编码 |
| function_name | VARCHAR(128) | Function Calling 函数名 |
| function_description | VARCHAR(512) | 函数描述 |
| parameters_schema | MEDIUMTEXT | JSON Schema 定义 |
| version | VARCHAR(32) | 版本号 |
| status | TINYINT | 状态：1 启用，0 停用 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| deleted | TINYINT | 逻辑删除 |

索引建议：

```sql
idx_skill_code(skill_code)
idx_function_name(function_name)
idx_status(status)
```

---

### 5.4 脚本规则表：`cr_script_rule`

| 字段名 | 类型 | 注释 |
|---|---|---|
| id | BIGINT PK | 主键 |
| script_name | VARCHAR(128) | 脚本名称 |
| script_code | VARCHAR(64) | 脚本编码 |
| script_language | VARCHAR(32) | 脚本语言：SHELL/PYTHON/NODE |
| script_content | MEDIUMTEXT | 脚本内容 |
| parameter_template | TEXT | 参数模板 |
| timeout_seconds | INT | 超时时间 |
| generated_by_ai | TINYINT | 是否由 AI 生成 |
| status | TINYINT | 状态：1 启用，0 停用 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| deleted | TINYINT | 逻辑删除 |

---

### 5.5 检视任务表：`cr_review_task`

| 字段名 | 类型 | 注释 |
|---|---|---|
| id | BIGINT PK | 主键 |
| task_no | VARCHAR(64) | 任务编号 |
| project_id | BIGINT | 项目 ID |
| project_name | VARCHAR(128) | 项目名称冗余 |
| trigger_type | VARCHAR(32) | 触发方式：MANUAL/SCHEDULE |
| review_branch | VARCHAR(128) | 检视分支 |
| review_days | INT | 检视最近 N 天 |
| commit_count | INT | 提交数量 |
| diff_file_count | INT | diff 文件数量 |
| issue_count | INT | 问题总数 |
| blocker_count | INT | blocker 数量 |
| critical_count | INT | critical 数量 |
| major_count | INT | major 数量 |
| minor_count | INT | minor 数量 |
| info_count | INT | info 数量 |
| ai_call_count | INT | AI 调用次数 |
| status | VARCHAR(32) | 任务状态 |
| start_time | DATETIME | 开始时间 |
| end_time | DATETIME | 结束时间 |
| error_message | TEXT | 错误信息 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| deleted | TINYINT | 逻辑删除 |

索引建议：

```sql
idx_task_no(task_no)
idx_project_id(project_id)
idx_status(status)
idx_create_time(create_time)
```

---

### 5.6 检视问题表：`cr_review_issue`

| 字段名 | 类型 | 注释 |
|---|---|---|
| id | BIGINT PK | 主键 |
| task_id | BIGINT | 任务 ID |
| project_id | BIGINT | 项目 ID |
| rule_id | BIGINT | 命中的规则 ID |
| skill_id | BIGINT | 关联 Skill ID |
| issue_source | VARCHAR(32) | 来源：AI/SCRIPT |
| severity | VARCHAR(32) | 严重等级 |
| issue_type | VARCHAR(64) | 问题类型 |
| file_path | VARCHAR(512) | 文件路径 |
| start_line | INT | 起始行 |
| end_line | INT | 结束行 |
| summary | VARCHAR(512) | 问题摘要 |
| detail | TEXT | 问题详情 |
| suggestion | TEXT | 修改建议 |
| code_snippet | MEDIUMTEXT | 相关代码片段 |
| raw_response | MEDIUMTEXT | AI 或脚本原始响应 |
| status | VARCHAR(32) | 问题状态：OPEN/IGNORED/FIXED |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| deleted | TINYINT | 逻辑删除 |

索引建议：

```sql
idx_task_id(task_id)
idx_project_id(project_id)
idx_rule_id(rule_id)
idx_severity(severity)
idx_status(status)
```

---

### 5.7 通知配置表：`cr_notify_config`

| 字段名 | 类型 | 注释 |
|---|---|---|
| id | BIGINT PK | 主键 |
| config_name | VARCHAR(128) | 配置名称 |
| channel_type | VARCHAR(32) | 渠道类型：WEBHOOK |
| webhook_url | VARCHAR(1024) | Webhook 地址 |
| secret_encrypt | VARCHAR(1024) | Webhook 密钥，加密存储 |
| enabled | TINYINT | 是否启用 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| deleted | TINYINT | 逻辑删除 |

---

### 5.8 通知模板表：`cr_notify_template`

| 字段名 | 类型 | 注释 |
|---|---|---|
| id | BIGINT PK | 主键 |
| template_name | VARCHAR(128) | 模板名称 |
| channel_type | VARCHAR(32) | 渠道类型 |
| event_type | VARCHAR(64) | 事件类型：TASK_FINISHED/TASK_FAILED |
| title_template | VARCHAR(512) | 标题模板 |
| content_template | MEDIUMTEXT | 内容模板 |
| status | TINYINT | 状态 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| deleted | TINYINT | 逻辑删除 |

模板变量示例：

```text
${projectName}
${taskNo}
${issueCount}
${blockerCount}
${criticalCount}
${majorCount}
${minorCount}
${infoCount}
${startTime}
${endTime}
${taskStatus}
```

---

### 5.9 分布式锁表：`cr_distributed_lock`

| 字段名 | 类型 | 注释 |
|---|---|---|
| id | BIGINT PK | 主键 |
| lock_key | VARCHAR(128) | 锁 Key |
| lock_value | VARCHAR(128) | 锁值，实例标识 |
| expire_time | DATETIME | 过期时间 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| deleted | TINYINT | 逻辑删除 |

唯一索引：

```sql
uk_lock_key(lock_key, deleted)
```

---

### 5.10 系统密钥配置表：`cr_secret_config`

| 字段名 | 类型 | 注释 |
|---|---|---|
| id | BIGINT PK | 主键 |
| secret_name | VARCHAR(128) | 密钥名称 |
| secret_type | VARCHAR(64) | 密钥类型：DEEPSEEK_API_KEY/WEBHOOK_SECRET |
| secret_value_encrypt | VARCHAR(2048) | 加密后的密钥 |
| status | TINYINT | 状态 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| deleted | TINYINT | 逻辑删除 |

说明：

- 项目访问令牌使用优先级高于默认访问令牌
- 若项目未配置单独访问令牌，则回退到系统默认访问令牌
- 系统默认访问令牌当前由管理员手动维护，后续支持账号密码自动获取

### 5.11 系统参数配置表：`cr_system_config`

| 字段名 | 类型 | 注释 |
|---|---|---|
| id | BIGINT PK | 主键 |
| config_key | VARCHAR(128) | 配置 Key，例如 DEFAULT_GITEE_TOKEN |
| config_value | TEXT | 配置值，默认访问令牌明文存储 |
| config_desc | VARCHAR(512) | 配置说明 |
| status | TINYINT | 状态 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| deleted | TINYINT | 逻辑删除 |

说明：

- 当前阶段由管理员每天手动更新
- 项目未配置单独访问令牌，或 `use_default_token = 1` 时，使用该默认令牌
- 后续增加账号密码后，可由自动任务刷新该配置值

---

## 6. AI 检视引擎详细设计

## 6.1 检视流程总览

```text
创建任务
  ↓
加载项目配置
  ↓
解析仓库访问令牌：优先使用项目单独令牌，否则使用系统默认令牌
  ↓
拉取 / 更新本地仓库
  ↓
计算最近 N 天 commits
  ↓
生成 diff
  ↓
加载匹配项目类型的启用规则
  ↓
AI 规则执行
  ↓
脚本规则执行
  ↓
合并问题结果
  ↓
问题去重
  ↓
保存问题
  ↓
更新任务统计
  ↓
推送通知
```

---

## 6.2 Git diff 获取策略

### 6.2.1 本地仓库目录

建议本地缓存目录：

```text
/data/code-review/repos/{projectId}/
```

Windows 开发环境可配置为：

```text
D:/code-review/repos/{projectId}/
```

### 6.2.2 拉取逻辑

首次执行：

```text
git clone -b {defaultBranch} {repoUrl} {localPath}
```

后续执行：

```text
git fetch origin
git checkout {defaultBranch}
git pull origin {defaultBranch}
```

### 6.2.3 最近 N 天提交

获取提交：

```text
git log --since="{N} days ago" --pretty=format:"%H"
```

获取 diff：

```text
git diff {oldestCommit}^..HEAD
```

也可以按提交逐个生成 diff，便于追踪问题来源：

```text
git show --format=fuller --stat --patch {commitHash}
```

推荐第一期使用“合并 diff”：

- 实现简单
- AI 上下文更连续
- 任务级别统计清晰

后续可扩展为“按提交检视”。

---

## 6.3 Prompt 组装策略

### 6.3.1 Prompt 输入组成

每次 AI 检视请求由以下部分组成：

1. System Prompt
2. 规则 Prompt 模板
3. 项目信息
4. 检视范围说明
5. Git diff 内容
6. 输出要求
7. Skill / Function Calling 定义

---

### 6.3.2 System Prompt 示例

```text
你是一个资深代码审查专家，擅长从 Git diff 中识别安全性、性能、代码风格和工程质量问题。

你必须遵守以下要求：
1. 只分析本次提供的 Git diff。
2. 不要臆测 diff 之外的代码。
3. 如果没有发现问题，应返回空 issues 数组。
4. 所有问题必须通过指定的 function calling 返回。
5. 不要直接输出自然语言结论。
6. 每个问题必须包含文件路径、行号、严重等级、摘要和修改建议。
```

---

### 6.3.3 规则 Prompt 模板示例

以 SQL 注入规则为例：

```text
请检查以下代码 diff 是否存在 SQL 注入风险。

重点关注：
1. 是否存在字符串拼接 SQL。
2. 是否直接将用户输入拼接进 SQL。
3. 是否绕过 MyBatis 参数绑定。
4. 是否存在 ${} 动态 SQL 使用不当。
5. 是否存在未经过白名单校验的排序字段、表名、列名拼接。

如果发现问题，请通过 function calling 返回结构化问题。
```

---

### 6.3.4 项目信息片段

```text
项目信息：
- 项目名称：${projectName}
- 项目类型：${projectType}
- 仓库地址：${repoUrl}
- 分支：${branch}
- 检视范围：最近 ${reviewDays} 天提交
```

---

### 6.3.5 Diff 内容片段

```text
以下是 Git diff 内容：

```diff
${diffContent}
```
```

---

### 6.3.6 完整 Prompt 组装结构

```text
[System Prompt]

[Rule Prompt]
${rule.promptTemplate}

[Project Context]
项目名称：${projectName}
项目类型：${projectType}
分支：${branch}
检视范围：最近 ${reviewDays} 天提交

[Review Scope]
只允许检查本次 diff 中新增或修改的代码。
不要报告未出现在 diff 中的问题。

[Git Diff]
${diffContent}

[Output Requirement]
你必须调用 function：${functionName}
返回结构化问题数据。
如果没有问题，issues 返回空数组。
```

---

## 6.4 Diff 分片策略

由于大模型上下文长度有限，需要对 diff 进行分片。

### 6.4.1 分片维度

推荐优先级：

1. 按文件分片
2. 单文件过大时按 hunk 分片
3. 仍过大时按 token 长度截断并标记

### 6.4.2 分片规则

```text
一个 AI 请求 = 一个规则 + 一个 diff 分片
```

例如：

```text
项目 A 有 5 条 AI 规则，diff 被切为 3 片
则最多发起 15 次 AI 调用
```

### 6.4.3 分片上下文

每个分片需要携带：

- 项目信息
- 规则 Prompt
- 当前文件路径
- 当前 diff 片段
- Skill 定义

---

## 6.5 Function Calling JSON Schema 示例

### 6.5.1 Skill 示例：标准代码问题上报

Skill 名称：

```text
标准代码检视问题上报
```

Function name：

```text
report_code_review_issues
```

Function description：

```text
用于返回代码检视过程中发现的问题列表，包括问题位置、严重等级、问题摘要、详细说明和修改建议。
```

完整 Function Calling 定义：

```json
{
  "type": "function",
  "function": {
    "name": "report_code_review_issues",
    "description": "用于返回代码检视过程中发现的问题列表，包括问题位置、严重等级、问题摘要、详细说明和修改建议。",
    "parameters": {
      "type": "object",
      "properties": {
        "issues": {
          "type": "array",
          "description": "代码检视发现的问题列表。如果没有问题，返回空数组。",
          "items": {
            "type": "object",
            "properties": {
              "severity": {
                "type": "string",
                "description": "严重等级",
                "enum": [
                  "blocker",
                  "critical",
                  "major",
                  "minor",
                  "info"
                ]
              },
              "issueType": {
                "type": "string",
                "description": "问题类型，例如 SQL_INJECTION、NULL_POINTER、PERFORMANCE、STYLE"
              },
              "filePath": {
                "type": "string",
                "description": "问题所在文件路径"
              },
              "startLine": {
                "type": "integer",
                "description": "问题起始行号"
              },
              "endLine": {
                "type": "integer",
                "description": "问题结束行号"
              },
              "summary": {
                "type": "string",
                "description": "问题摘要，简短描述问题"
              },
              "detail": {
                "type": "string",
                "description": "问题详细说明，解释为什么这是问题"
              },
              "suggestion": {
                "type": "string",
                "description": "具体修改建议"
              },
              "codeSnippet": {
                "type": "string",
                "description": "相关代码片段"
              },
              "confidence": {
                "type": "number",
                "description": "置信度，取值范围 0 到 1"
              }
            },
            "required": [
              "severity",
              "issueType",
              "filePath",
              "startLine",
              "summary",
              "detail",
              "suggestion"
            ]
          }
        }
      },
      "required": [
        "issues"
      ]
    }
  }
}
```

---

## 6.6 DeepSeek OpenAI 兼容请求示例

```json
{
  "model": "deepseek-chat",
  "messages": [
    {
      "role": "system",
      "content": "你是一个资深代码审查专家..."
    },
    {
      "role": "user",
      "content": "请检查以下代码 diff 是否存在 SQL 注入风险..."
    }
  ],
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "report_code_review_issues",
        "description": "用于返回代码检视过程中发现的问题列表...",
        "parameters": {
          "type": "object",
          "properties": {
            "issues": {
              "type": "array",
              "items": {
                "type": "object"
              }
            }
          },
          "required": [
            "issues"
          ]
        }
      }
    }
  ],
  "tool_choice": {
    "type": "function",
    "function": {
      "name": "report_code_review_issues"
    }
  },
  "temperature": 0.1
}
```

说明：

- `temperature` 设置较低，保证输出稳定
- 使用 `tool_choice` 强制模型调用指定 function
- 不依赖自然语言解析
- 每条规则可以绑定不同 Skill

---

## 6.7 AI 返回结果解析

DeepSeek 返回示例：

```json
{
  "choices": [
    {
      "message": {
        "role": "assistant",
        "tool_calls": [
          {
            "type": "function",
            "function": {
              "name": "report_code_review_issues",
              "arguments": "{\"issues\":[{\"severity\":\"critical\",\"issueType\":\"SQL_INJECTION\",\"filePath\":\"src/main/java/UserMapper.java\",\"startLine\":42,\"endLine\":44,\"summary\":\"存在SQL拼接风险\",\"detail\":\"该代码直接拼接用户输入到SQL语句中，可能导致SQL注入。\",\"suggestion\":\"请使用MyBatis参数绑定或预编译参数。\",\"codeSnippet\":\"sql += userName;\",\"confidence\":0.92}]}"
            }
          }
        ]
      }
    }
  ]
}
```

解析流程：

```text
读取 choices[0].message.tool_calls
    ↓
校验 function.name 是否等于规则关联 Skill 的 function_name
    ↓
解析 function.arguments JSON 字符串
    ↓
校验 issues 数组
    ↓
字段合法性校验
    ↓
严重等级标准化
    ↓
行号和文件路径校验
    ↓
保存 cr_review_issue
```

---

## 6.8 结果持久化流程

```text
AI 返回 function arguments
    ↓
JSON 解析为 ReviewIssueDTO
    ↓
校验必填字段
    ↓
补充 task_id / project_id / rule_id / skill_id
    ↓
问题去重
    ↓
保存 cr_review_issue
    ↓
更新 cr_review_task 统计字段
```

---

## 6.9 问题去重策略

同一任务内，问题唯一键建议：

```text
task_id + rule_id + file_path + start_line + issue_type + summary_hash
```

处理方式：

- 完全重复：忽略
- 同文件同行但描述不同：保留
- 不同规则发现同一问题：保留，便于追踪规则命中情况

---

## 6.10 脚本规则执行设计

脚本输入参数：

```json
{
  "projectId": 1,
  "projectName": "用户中心",
  "repoPath": "/data/code-review/repos/1",
  "branch": "master",
  "diffFile": "/data/code-review/tasks/TASK001/diff.patch"
}
```

脚本标准输出格式：

```json
{
  "issues": [
    {
      "severity": "major",
      "issueType": "CUSTOM_SCRIPT",
      "filePath": "src/main/java/Demo.java",
      "startLine": 10,
      "endLine": 12,
      "summary": "脚本发现潜在问题",
      "detail": "详细说明",
      "suggestion": "修改建议",
      "codeSnippet": "相关代码"
    }
  ]
}
```

安全限制：

- 设置执行超时时间
- 限制工作目录
- 限制环境变量
- 不将系统密钥传入脚本环境
- 脚本执行日志单独记录
- 第一阶段建议仅允许运维或管理员配置脚本

---

## 7. 定时调度与防重机制

## 7.1 技术选型

由于当前部署形态暂时为单体应用，第一期可使用：

```text
Spring Scheduler + ThreadPoolTaskExecutor + MySQL 分布式锁
```

虽然当前是单体，但仍建议引入 MySQL 分布式锁，方便未来平滑扩展到多实例部署。

---

## 7.2 定时任务配置

配置项示例：

```yaml
code-review:
  schedule:
    enabled: true
    cron: "0 0 2 * * ?"
    default-review-days: 1
```

默认每天凌晨 2 点执行。

---

## 7.3 防重场景

需要防止以下重复：

1. 同一时间多个调度实例重复执行
2. 用户连续点击手动触发
3. 同一项目已有运行中任务时再次创建任务

---

## 7.4 分布式锁设计

锁 Key 示例：

```text
SCHEDULE_REVIEW_DAILY
PROJECT_REVIEW_RUNNING:{projectId}
```

加锁逻辑：

```text
insert lock_key
成功：获得锁
失败：判断 expire_time 是否过期
    已过期：更新 lock_value 和 expire_time
    未过期：获取锁失败
```

释放锁：

```text
任务结束后根据 lock_key + lock_value 删除或更新 deleted = 1
```

锁超时时间：

- 调度总锁：2 小时
- 单项目检视锁：1 小时
- 可配置

---

## 7.5 手动触发防重

当用户手动发起检视时：

```text
检查该 project_id 是否存在 RUNNING/PENDING 任务
    ↓
存在：拒绝创建，提示“当前项目已有检视任务正在执行”
    ↓
不存在：创建新任务
```

---

## 7.6 异步执行设计

使用线程池：

```java
@Bean
public ThreadPoolTaskExecutor reviewTaskExecutor() {
    ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
    executor.setCorePoolSize(4);
    executor.setMaxPoolSize(8);
    executor.setQueueCapacity(200);
    executor.setThreadNamePrefix("review-task-");
    executor.initialize();
    return executor;
}
```

异步任务：

```text
ReviewTaskAppService.createTask()
    ↓
ReviewEngineAppService.submitAsync(taskId)
    ↓
ReviewEngineWorker.run(taskId)
```

---

## 8. 消息推送设计

## 8.1 渠道抽象

```java
public interface NotifyChannel {
    String supportChannel();

    NotifySendResult send(NotifySendCommand command);
}
```

Webhook 实现：

```java
public class WebhookNotifyChannel implements NotifyChannel {
    @Override
    public String supportChannel() {
        return "WEBHOOK";
    }

    @Override
    public NotifySendResult send(NotifySendCommand command) {
        // HTTP POST webhookUrl
    }
}
```

---

## 8.2 Webhook 请求体

建议统一 POST JSON：

```json
{
  "title": "代码检视完成：用户中心",
  "content": "本次检视发现 12 个问题，其中 critical 2 个，major 5 个。",
  "eventType": "TASK_FINISHED",
  "projectId": 1,
  "projectName": "用户中心",
  "taskNo": "CR202605100001",
  "taskStatus": "SUCCESS",
  "summary": {
    "issueCount": 12,
    "blockerCount": 0,
    "criticalCount": 2,
    "majorCount": 5,
    "minorCount": 4,
    "infoCount": 1
  },
  "timestamp": "2026-05-10 10:00:00"
}
```

---

## 8.3 通知模板渲染

模板示例：

```text
项目【${projectName}】代码检视已完成。

任务编号：${taskNo}
任务状态：${taskStatus}
问题总数：${issueCount}

严重度分布：
- Blocker：${blockerCount}
- Critical：${criticalCount}
- Major：${majorCount}
- Minor：${minorCount}
- Info：${infoCount}

开始时间：${startTime}
结束时间：${endTime}
```

模板渲染方式：

- 第一阶段可使用简单变量替换
- 后续可引入 FreeMarker / Velocity
- 模板变量必须白名单化，避免任意表达式执行风险

---

## 8.4 触发时机

| 事件 | 是否通知 | 说明 |
|---|---:|---|
| 任务完成成功 | 是 | 推送问题汇总 |
| 任务执行失败 | 是 | 推送失败原因 |
| 发现 blocker 问题 | 可配置 | 可实时推送 |
| 定时任务开始 | 否 | 第一阶段不推送 |
| 手动任务创建 | 否 | 第一阶段不推送 |

---

## 8.5 通知失败处理

通知发送失败时：

- 不影响检视任务成功状态
- 记录通知失败日志
- 可在任务详情页展示通知状态
- 后续可增加手动重试

---

## 9. 关键 API 列表

要求：

- 所有 API 均使用 POST
- 遵循 DDD 四层架构
- Controller 只做参数接收和响应转换
- Application Service 负责编排业务用例

统一响应格式：

```json
{
  "code": "0",
  "message": "success",
  "data": {}
}
```

分页响应：

```json
{
  "code": "0",
  "message": "success",
  "data": {
    "records": [],
    "total": 100,
    "pageNo": 1,
    "pageSize": 10
  }
}
```

---

## 9.1 项目管理 API

| URL | 方法 | 用途 |
|---|---|---|
| `/api/project/create` | POST | 新增项目 |
| `/api/project/update` | POST | 编辑项目 |
| `/api/project/delete` | POST | 删除项目 |
| `/api/project/detail` | POST | 项目详情 |
| `/api/project/page` | POST | 项目分页列表 |
| `/api/project/enable` | POST | 启用项目 |
| `/api/project/disable` | POST | 停用项目 |
| `/api/project/import-excel` | POST | Excel 导入项目 |
| `/api/project/test-repo-connection` | POST | 测试仓库连接 |

新增项目请求示例：

```json
{
  "projectName": "用户中心",
  "projectCode": "user-center",
  "projectType": "BACKEND",
  "repoUrl": "https://gitee.com/example/user-center.git",
  "repoToken": "xxx",
  "defaultBranch": "master",
  "ownerName": "张三",
  "reviewDays": 7
}
```

---

## 9.2 规则管理 API

| URL | 方法 | 用途 |
|---|---|---|
| `/api/rule/create` | POST | 新增规则 |
| `/api/rule/update` | POST | 编辑规则 |
| `/api/rule/delete` | POST | 删除规则 |
| `/api/rule/detail` | POST | 规则详情 |
| `/api/rule/page` | POST | 规则分页列表 |
| `/api/rule/enable` | POST | 启用规则 |
| `/api/rule/disable` | POST | 停用规则 |
| `/api/rule/generate-script` | POST | 通过 AI 生成脚本规则 |

新增 AI 规则请求示例：

```json
{
  "ruleName": "SQL 注入风险检查",
  "ruleCode": "SQL_INJECTION_CHECK",
  "ruleKind": "AI",
  "ruleType": "SECURITY",
  "severity": "critical",
  "projectType": "BACKEND",
  "promptTemplate": "请检查 diff 中是否存在 SQL 注入风险...",
  "skillId": 1
}
```

---

## 9.3 Skill 管理 API

| URL | 方法 | 用途 |
|---|---|---|
| `/api/skill/create` | POST | 新增 Skill |
| `/api/skill/update` | POST | 编辑 Skill |
| `/api/skill/delete` | POST | 删除 Skill |
| `/api/skill/detail` | POST | Skill 详情 |
| `/api/skill/page` | POST | Skill 分页列表 |
| `/api/skill/enable` | POST | 启用 Skill |
| `/api/skill/disable` | POST | 停用 Skill |
| `/api/skill/validate-schema` | POST | 校验 JSON Schema |

新增 Skill 请求示例：

```json
{
  "skillName": "标准代码问题上报",
  "skillCode": "REPORT_CODE_REVIEW_ISSUES",
  "functionName": "report_code_review_issues",
  "functionDescription": "用于返回代码检视过程中发现的问题列表",
  "parametersSchema": "{...}",
  "version": "1.0.0"
}
```

---

## 9.4 脚本规则 API

| URL | 方法 | 用途 |
|---|---|---|
| `/api/script/create` | POST | 新增脚本 |
| `/api/script/update` | POST | 编辑脚本 |
| `/api/script/delete` | POST | 删除脚本 |
| `/api/script/detail` | POST | 脚本详情 |
| `/api/script/page` | POST | 脚本分页列表 |
| `/api/script/test-run` | POST | 测试运行脚本 |

---

## 9.5 检视任务 API

| URL | 方法 | 用途 |
|---|---|---|
| `/api/review-task/manual-start` | POST | 手动发起检视 |
| `/api/review-task/cancel` | POST | 取消任务 |
| `/api/review-task/detail` | POST | 任务详情 |
| `/api/review-task/page` | POST | 任务分页列表 |
| `/api/review-task/statistics` | POST | 任务统计 |
| `/api/review-task/retry` | POST | 重试失败任务 |

手动触发请求示例：

```json
{
  "projectId": 1,
  "reviewDays": 7,
  "branch": "master"
}
```

---

## 9.6 问题管理 API

| URL | 方法 | 用途 |
|---|---|---|
| `/api/review-issue/page` | POST | 问题分页列表 |
| `/api/review-issue/detail` | POST | 问题详情 |
| `/api/review-issue/ignore` | POST | 忽略问题 |
| `/api/review-issue/mark-fixed` | POST | 标记已修复 |
| `/api/review-issue/statistics` | POST | 问题统计 |
| `/api/review-issue/export` | POST | 导出问题 |

---

## 9.7 通知配置 API

| URL | 方法 | 用途 |
|---|---|---|
| `/api/notify-config/create` | POST | 新增通知配置 |
| `/api/notify-config/update` | POST | 编辑通知配置 |
| `/api/notify-config/delete` | POST | 删除通知配置 |
| `/api/notify-config/page` | POST | 通知配置列表 |
| `/api/notify-config/test-send` | POST | 测试通知发送 |

---

## 9.8 通知模板 API

| URL | 方法 | 用途 |
|---|---|---|
| `/api/notify-template/create` | POST | 新增模板 |
| `/api/notify-template/update` | POST | 编辑模板 |
| `/api/notify-template/detail` | POST | 模板详情 |
| `/api/notify-template/page` | POST | 模板列表 |
| `/api/notify-template/preview` | POST | 模板预览 |

---

## 9.9 看板统计 API

| URL | 方法 | 用途 |
|---|---|---|
| `/api/dashboard/overview` | POST | 首页概览 |
| `/api/dashboard/issue-trend` | POST | 问题趋势 |
| `/api/dashboard/severity-distribution` | POST | 严重度分布 |
| `/api/dashboard/project-ranking` | POST | 项目问题排行 |
| `/api/dashboard/rule-hit-ranking` | POST | 规则命中排行 |

---

## 10. 前端页面结构

前端未指定技术栈，建议采用：

```text
Vue 3 + TypeScript + Vite + Element Plus / Naive UI + ECharts
```

理由：

- 管理后台生态成熟
- 页面开发效率高
- 组件美观现代
- 与 Spring Boot 后端对接简单
- ECharts 适合趋势图和分布图展示

也可使用 React + Ant Design Pro，但如果没有团队偏好，Vue3 后台系统落地成本更低。

---

## 10.1 页面整体布局

```text
顶部栏：
- 系统名称
- 当前用户
- 全局操作

左侧菜单：
- 首页看板
- 项目管理
- 检视规则
- AI Skill
- 脚本规则
- 检视任务
- 问题列表
- 通知配置
- 系统配置
```

---

## 10.2 首页看板

展示内容：

- 项目总数
- 启用项目数
- 今日检视任务数
- 今日发现问题数
- blocker / critical 问题数
- 最近 7 / 30 天问题趋势图
- 严重度分布饼图
- 项目问题排行
- 规则命中排行
- 最近任务列表

页面风格：

- 卡片式布局
- 图表可视化
- 严重等级颜色区分

严重等级颜色建议：

| 等级 | 颜色 |
|---|---|
| blocker | 深红 |
| critical | 红色 |
| major | 橙色 |
| minor | 蓝色 |
| info | 灰色 |

---

## 10.3 项目管理页面

功能：

- 项目列表
- 新增项目
- 编辑项目
- 删除项目
- 启用 / 停用
- Excel 导入
- 测试 Gitee 仓库连接
- 一键发起检视
- 查看项目历史任务

列表字段：

- 项目名称
- 项目类型
- 仓库地址
- 默认分支
- 责任人
- 最近检视天数
- 状态
- 最近检视时间
- 操作

---

## 10.4 检视规则页面

功能：

- 规则列表
- 新增 AI 规则
- 新增脚本规则
- 编辑规则
- 启用 / 停用
- 关联 Skill
- 规则 Prompt 编辑器
- 适用项目类型配置

Prompt 编辑器建议：

- 支持多行编辑
- 支持变量提示
- 支持模板预览

变量示例：

```text
${projectName}
${projectType}
${diffContent}
${branch}
${reviewDays}
```

---

## 10.5 AI Skill 页面

这是平台的核心特色页面，需要重点设计。

功能：

- Skill 列表
- 新增 Skill
- 编辑 Function name
- 编辑 Function description
- 编辑 JSON Schema
- Schema 校验
- Skill 版本管理
- 查看被哪些规则引用

页面设计建议：

```text
左侧：Skill 列表
右侧：Skill 详情编辑区
    - 基本信息
    - Function 定义
    - JSON Schema 编辑器
    - Schema 校验结果
    - 示例输出预览
```

JSON Schema 编辑器建议使用 Monaco Editor。

---

## 10.6 脚本规则页面

功能：

- 脚本列表
- 新增脚本
- 编辑脚本内容
- AI 生成脚本
- 测试运行
- 查看执行日志

页面设计：

- 脚本语言选择
- Monaco Editor 代码编辑
- 测试输入参数
- 测试输出 JSON
- 错误日志展示

---

## 10.7 检视任务页面

功能：

- 任务列表
- 任务详情
- 查看任务状态
- 查看统计信息
- 查看执行日志
- 重试失败任务
- 取消待执行任务

列表字段：

- 任务编号
- 项目名称
- 触发方式
- 分支
- 检视天数
- 状态
- 问题数量
- 开始时间
- 结束时间
- 操作

任务详情页：

```text
基础信息
    任务编号 / 项目 / 分支 / 触发方式 / 状态

统计信息
    问题总数 / 严重度分布 / AI 调用次数 / diff 文件数

问题列表
    文件 / 行号 / 严重等级 / 摘要 / 规则 / 来源

执行日志
    Git 拉取日志 / AI 调用摘要 / 脚本执行日志 / 错误信息
```

---

## 10.8 问题详情页面

功能：

- 查看问题摘要
- 查看问题详情
- 查看修改建议
- 查看代码片段
- 查看关联规则
- 查看来源：AI / SCRIPT
- 忽略问题
- 标记已修复

布局建议：

```text
顶部：问题摘要 + 严重等级标签
左侧：代码片段
右侧：问题详情、修改建议、规则信息
底部：原始 AI 响应
```

---

## 10.9 通知配置页面

功能：

- Webhook 配置
- 启用 / 停用通知
- 测试发送
- 通知模板编辑
- 模板预览

Webhook 配置字段：

- 配置名称
- Webhook URL
- Secret
- 是否启用

---

## 11. 安全设计

### 11.1 API Key 安全

要求：

- DeepSeek API Key 不允许硬编码
- 默认访问令牌允许明文配置，且支持每天手动更新
- 项目单独访问令牌允许明文存储，不做加密
- Webhook Secret 不允许明文存储

实现：

- DeepSeek API Key 使用 AES 加密存储
- Webhook Secret 使用 AES 加密存储
- 默认访问令牌和项目访问令牌暂按业务要求明文保存
- 加密主密钥通过环境变量注入
- 日志中禁止打印 DeepSeek API Key、Webhook Secret 和访问令牌

配置示例：

```yaml
code-review:
  crypto:
    master-key: ${CODE_REVIEW_MASTER_KEY}
```

---

### 11.2 敏感日志脱敏

需要脱敏的内容：

- 项目访问令牌（单独配置）
- 默认访问令牌（系统统一配置）

脱敏示例：

```text
sk-xxxxxxxxxxxx1234
```

---

### 11.3 脚本执行安全

脚本规则风险较高，需要控制：

- 仅管理员可创建脚本
- 脚本执行必须超时
- 脚本不能读取密钥环境变量
- 脚本工作目录限制在任务目录
- 禁止执行危险命令可作为后续增强
- 生产环境建议将脚本执行隔离到容器

第一阶段单体部署下，建议：

```text
脚本执行权限仅开放给管理员
脚本执行目录固定
脚本执行超时强制 kill
脚本输出大小限制
```

---

### 11.4 Webhook 安全

- Webhook URL 存储时可选择加密
- Secret 加密存储
- 发送时可增加签名头

签名示例：

```text
X-CodeReview-Timestamp: 1715320000
X-CodeReview-Signature: HMAC-SHA256(timestamp + body, secret)
```

---

### 11.5 接口权限

虽然需求未明确用户体系，但生产环境建议预留：

- 管理员
- 普通用户
- 只读用户

权限示例：

| 功能 | 管理员 | 普通用户 | 只读用户 |
|---|---:|---:|---:|
| 项目查看 | 是 | 是 | 是 |
| 项目编辑 | 是 | 是 | 否 |
| 规则配置 | 是 | 否 | 否 |
| 脚本配置 | 是 | 否 | 否 |
| 手动触发检视 | 是 | 是 | 否 |
| 查看问题 | 是 | 是 | 是 |

---

## 12. 性能与扩展性设计

### 12.1 异步化

以下操作必须异步：

- Git clone / pull
- Git diff 生成
- AI 调用
- 脚本执行
- 通知推送

前端发起检视后立即返回任务编号：

```json
{
  "taskNo": "CR202605100001",
  "status": "PENDING"
}
```

用户通过任务列表查看进度。

---

### 12.2 AI 调用限流

需要控制：

- 单任务最大 AI 调用次数
- 单项目并发数
- 全局 AI 并发数
- 单次 diff 最大长度
- 单个文件最大长度

建议配置：

```yaml
code-review:
  ai:
    max-concurrent: 4
    max-calls-per-task: 100
    max-diff-chars-per-request: 30000
    timeout-seconds: 60
```

---

### 12.3 大 diff 处理

策略：

1. 优先按文件拆分
2. 超大文件按 hunk 拆分
3. 忽略二进制文件
4. 忽略 lock 文件可配置
5. 忽略自动生成文件可配置

默认忽略：

```text
*.lock
package-lock.json
yarn.lock
dist/**
target/**
node_modules/**
```

---

### 12.4 未来扩展到集群

当前按单体部署设计，但已预留：

- MySQL 分布式锁
- 任务状态表
- 异步任务可迁移到 MQ
- 本地仓库目录可迁移到共享存储
- AI 调用客户端无状态
- 通知模块无状态

未来可演进为：

```text
Web 节点
    ↓
任务队列 MQ
    ↓
Review Worker 集群
    ↓
MySQL + 对象存储
```

---

## 13. 核心枚举设计

### 13.1 项目类型

```java
public enum ProjectType {
    FRONTEND,
    BACKEND
}
```

### 13.2 规则种类

```java
public enum RuleKind {
    AI,
    SCRIPT
}
```

### 13.3 规则类型

```java
public enum RuleType {
    SECURITY,
    PERFORMANCE,
    STYLE,
    CUSTOM
}
```

### 13.4 严重等级

```java
public enum Severity {
    BLOCKER,
    CRITICAL,
    MAJOR,
    MINOR,
    INFO
}
```

### 13.5 任务状态

```java
public enum ReviewTaskStatus {
    PENDING,
    RUNNING,
    SUCCESS,
    FAILED,
    CANCELED
}
```

### 13.6 问题状态

```java
public enum ReviewIssueStatus {
    OPEN,
    IGNORED,
    FIXED
}
```

---

## 14. 推荐落地顺序

为了便于后续直接按 Claude 分步骤实现，下面将设计进一步拆成可执行阶段，每一阶段都能独立交付。

### 第 0 阶段：基础工程初始化

目标：搭建可运行骨架。

包含：

1. Spring Boot 工程初始化
2. DDD 四层目录结构
3. MyBatis-Plus 基础配置
4. MySQL 连接配置
5. 统一返回体与异常处理
6. 日志与配置中心基础
7. 通用枚举、常量、工具类

### 第 1 阶段：项目与令牌管理

目标：完成项目基础数据闭环。

包含：

1. 项目新增、编辑、删除、启停
2. 项目类型维护
3. 项目单独访问令牌配置（明文）
4. 系统默认访问令牌配置（明文）
5. 默认令牌每日手动更新入口
6. 后续账号密码自动获取令牌的预留接口
7. Excel 导入项目
8. 项目仓库连接测试

### 第 2 阶段：AI Skill 与规则管理

目标：完成 AI 规则建模能力。

包含：

1. Skill 新增、编辑、启停
2. JSON Schema 编辑与校验
3. AI 规则新增、编辑、启停
4. AI 规则与 Skill 绑定
5. 脚本规则新增、编辑、启停
6. AI 生成脚本入口

### 第 3 阶段：检视任务引擎

目标：完成检视核心链路。

包含：

1. 手动发起检视
2. 定时触发检视
3. 最近 N 天提交获取
4. Git clone / pull / diff
5. diff 分片
6. AI Function Calling 调用
7. 脚本规则执行
8. 问题解析与落库
9. 任务统计更新
10. 异步线程池

### 第 4 阶段：通知与历史查询

目标：完成任务结果闭环。

包含：

1. Webhook 通知配置
2. 通知模板配置
3. 任务完成通知
4. 任务失败通知
5. 任务历史分页
6. 问题列表分页
7. 问题详情查看
8. 问题忽略 / 修复标记

### 第 5 阶段：前端管理后台

目标：完成可操作后台页面。

包含：

1. 首页看板
2. 项目管理页
3. 规则管理页
4. AI Skill 页
5. 脚本规则页
6. 检视任务页
7. 问题详情页
8. 通知配置页
9. 图表统计页

### 第 6 阶段：生产增强

目标：补齐生产能力。

包含：

1. 分布式锁
2. AI 调用限流
3. 大 diff 优化
4. 脚本沙箱隔离
5. 审计日志
6. 权限控制
7. 多实例扩展预留

---

## 15. Claude 实现拆分建议

为便于后续用 Claude 按步骤编码，建议将整个系统拆成以下实现单元，每个单元都能独立交付、独立验证。

### 15.1 第一批实现单元：工程底座

1. 初始化 Spring Boot 2.7 + JDK 17 + MyBatis-Plus 工程
2. 建立 DDD 四层目录结构
3. 配置 MySQL、Swagger/OpenAPI、日志、统一响应
4. 补齐公共枚举、异常、工具类

### 15.2 第二批实现单元：基础数据模块

1. 项目管理模块
2. 默认访问令牌配置模块
3. 项目单独访问令牌配置模块
4. Excel 导入项目功能
5. 项目仓库连接测试功能

### 15.3 第三批实现单元：AI 配置模块

1. AI Skill 模块
2. JSON Schema 校验
3. AI 规则模块
4. 脚本规则模块
5. AI 生成脚本接口

### 15.4 第四批实现单元：检视核心链路

1. 检视任务创建与状态流转
2. Git 拉取与 diff 获取
3. diff 分片与规则匹配
4. DeepSeek Function Calling 适配
5. 问题解析和落库
6. 任务统计更新

### 15.5 第五批实现单元：通知与查询

1. Webhook 通知配置
2. 通知模板渲染
3. 任务历史列表
4. 问题详情与状态变更
5. 统计接口

### 15.6 第六批实现单元：前端页面

1. 项目列表页
2. 规则配置页
3. Skill 编辑页
4. 任务历史页
5. 问题详情页
6. 看板首页

### 15.7 Claude 执行方式建议

建议后续按下面顺序让 Claude 实现：

1. 先生成数据库表和实体
2. 再生成后端分层代码
3. 再生成 API 和 Service
4. 再生成检视引擎
5. 再生成前端页面
6. 最后补测试和联调

这样可以避免一次性生成过大范围代码，便于逐步校验和修正。

---

## 16. 总结

本方案以 **Spring Boot 2.7 + MyBatis-Plus + MySQL 5.7** 为后端基础，采用 DDD 四层架构组织代码，通过异步任务机制完成 Git 拉取、diff 分析、AI 调用和脚本执行。

核心设计重点是 **AI Skill**：

- 每条 AI 检视规则都可以绑定一个 Skill
- Skill 对应 DeepSeek OpenAI 兼容接口中的 Function Calling 定义
- 通过 JSON Schema 强约束模型输出
- 避免自然语言解析不稳定
- 实现问题数据的标准化、可统计、可追踪和可扩展

当前部署形态按单体应用设计，但通过 MySQL 分布式锁、任务状态表、异步执行器和无状态 AI 客户端预留了未来集群化扩展能力。

通知模块第一期实现 Webhook，并通过渠道接口抽象为后续企业微信、钉钉、邮件等渠道扩展做好准备。

项目访问令牌的当前策略为：

- 系统默认访问令牌统一维护，且每天手动更新
- 项目可单独配置访问令牌
- 当前阶段访问令牌不加密，便于日常维护
- 后续具备账号密码后，可扩展自动获取令牌流程

