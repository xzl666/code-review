# 剩余工作清单

更新时间：2026-05-18

说明：密钥加解密不再作为当前项目待办，DeepSeek API Key、Gitee Token、Webhook Secret 按 MVP 配置方式处理。

## 执行顺序

1. 前端 AI Skill 管理页（已完成）
   - 接入 Skill 分页、新增、编辑、删除、启停。
   - 接入 JSON Schema 校验。
   - 展示函数名、版本、状态和 Schema 摘要。

2. 前端检视规则管理页（已完成）
   - 接入规则分页、新增、编辑、删除、启停。
   - 支持 AI 规则绑定 Skill，脚本规则绑定脚本。
   - 支持项目类型、严重度、规则类型、Prompt 模板维护。
   - `/api/rule/generate-script` 当前仍未启用，仅保留入口提示。

3. 前端脚本规则管理页（已完成）
   - 接入脚本分页、新增、编辑、删除、启停。
   - 支持 Shell、Python、Node 脚本内容维护。
   - 接入测试运行，展示 stdout、stderr、退出码、超时状态。
   - 后端脚本执行仍缺少生产级沙箱隔离，当前仅适合可信脚本。

4. 前端通知配置页（已完成）
   - 接入 Webhook 配置分页、新增、编辑、删除、启停、测试发送。
   - 接入通知模板分页、新增、编辑、删除、启停、预览。
   - 后端已触发任务成功/失败通知，但缺少投递日志和失败重试队列。

5. 任务和问题闭环增强（已完成）
   - 检视任务页已增加手动发起任务、任务详情抽屉、运行中任务自动刷新。
   - 问题列表页已增加详情抽屉、代码片段、修复建议、项目/任务/来源过滤和导出入口。
   - 后端已增加启动时自动关闭超过 2 小时的历史 RUNNING 任务。

6. 大仓库与生产化增强（已完成 MVP 闭环）
   - diff chunk 已升级为按文件头与 hunk 边界切分，并携带 old/new 起始行元数据。
   - 已补充 `DiffChunkServiceTest` 单元测试，覆盖 hunk 边界和行号元数据。
   - skipped commit/file warning 已拆成 `skipped_commit_count`、`skipped_file_count`、`warning_message` 独立字段，并提供启动时自动迁移。
   - 已补充 `GitDiffServiceWarningTest` 单元测试，覆盖 skipped warning 生成。
   - 后续仍可继续补充 AI 响应解析、通知发送、任务状态流转的集成测试。
