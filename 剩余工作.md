# 剩余工作清单

更新时间：2026-07-16

## 当前结论

核心代码链路已经具备：项目配置 -> Gitee SSH 拉取 -> OCR 检视 -> 大模型调用 -> 问题与 Token 统计落库 -> 报告生成 -> 按人员展示/通知。当前环境已有一次可核验的手动提交检视成功记录（任务 52，87 次调用全部成功，434,464 Token），因此“手动提交检视”的主链路能够运行。

整个业务流程还不能认定为完整验收通过。每天 1 点的“昨天代码”任务、提交人归属、多人通知、Linux 免安装部署尚未完成一次联合实测；权限和密钥处理也不满足生产要求。

## 已核验能力

- [x] 后端接口和前端开发服务可访问，Gitee SSH 私钥状态为已配置。
- [x] 已启用一个 OpenAI 兼容模型配置，OCR 能取得模型地址、模型名和 API Key。
- [x] 手动 `COMMIT` 模式真实执行成功，并记录调用次数、成功数、失败数和 Token。
- [x] OCR 源码已纳入 `vendor/open-code-review`，Windows/Linux 的 amd64/arm64 二进制已打入 JAR。
- [x] 项目 Excel 导入、人员切换、管理员、多人负责人、问题责任人字段和招乎测试发送入口已实现。
- [x] 后端 `mvn test -q` 通过，前端 `npm run build` 通过。
- [x] 旧引擎产生的 45 条历史问题及缓存报告已清理，关联任务问题统计已归零。

## P0：试运行前必须完成

1. 完成“昨天代码”联合验收
   - 为测试项目配置至少一名系统内负责人并启用定时检视。
   - 准备昨天存在提交的小仓库，提交信息包含姓名或工号。
   - 手动选择“检视昨天提交的代码”，先关闭通知验证 OCR、问题归属和报告，再开启通知验证逐人内容与链接。
   - 等待或模拟凌晨 1 点调度，确认每个项目各生成一条 `SCHEDULE` 任务。当前数据库中定时任务数量为 0，尚无成功证据。

2. 补齐真正的身份认证和接口鉴权
   - 当前身份仅来自浏览器可修改的 `X-User-Id`，任何人都可以切换成管理员，不能作为生产登录。
   - 普通用户目前仍能查看全部任务，并可直接读取包含全部问题的任务报告。需要统一限制项目、任务、报告、导出、配置修改等接口。
   - 接入内部统一认证后，由服务端生成可信用户上下文；管理员和项目负责人权限必须在后端校验。

3. 清理并轮换敏感配置
   - `application.yml` 中存在招乎默认 Client Secret，必须删除默认明文并轮换已暴露凭据。
   - 模型 API Key、Gitee 私钥目前存于数据库或本地文件，尚未加密；生产环境应接入环境变量、CI Secret 或内部密钥服务。
   - 将 `code-review.ai.debug-log-enabled` 的生产默认值改为 `false`，避免代码、Prompt 或响应进入日志。

4. 修正项目负责人和调度状态
   - 当前唯一项目 `ruoyi-vue-pro4` 的负责人关系为空，历史 `ownerName=xuzilang` 未迁移到徐梓琅；无匹配提交人时将无人收到通知。
   - 调度器遍历的是所有启用项目，未读取 `scheduleEnabled`；界面开关和实际行为不一致。需要确定“所有项目强制定时”或“按项目开关”，并只保留一种语义。

## P1：上线前完成

1. 提升提交人匹配可靠性
   - 现在通过 `git blame` 的作者姓名/邮箱匹配中文姓名或工号。英文 Git 用户名、别名、共享邮箱、删除行和无法 blame 的行会成为“未匹配”。
   - 增加 Git 身份别名表（姓名、工号、邮箱、Git author），并提供未匹配问题的人工分配和批量修正能力。
   - 历史问题已经清理，后续仅需验证新任务能够稳定匹配责任人。

2. 完善报告和通知
   - 报告已经改为按 `reviewMode` 展示触发方式、检视方式、版本/时间范围、多人负责人、模型调用和 Token；待用五种检视方式分别回归展示结果。
   - 招乎投递日志页面只翻译 `TASK_SUCCESS/TASK_FAILED`，新流程写入的是 `DAILY_REVIEW_SUCCESS/DAILY_REVIEW_FAILED`，需统一枚举和筛选项。
   - 招乎通知中的严重度、状态等展示统一为中文；测试发送也应记录审计日志。
   - 明确无问题、任务失败、无负责人、未匹配责任人时的接收人兜底策略。

3. 建立端到端自动化测试
   - 增加带本地 Git 仓库和假 OCR/假模型的集成测试，覆盖四种 OCR 模式及 `YESTERDAY`。
   - 覆盖任务状态流转、问题归属、负责人可见、普通用户不可越权、报告权限、通知分组及失败重试。
   - 覆盖 Excel 导入的部分失败、重复项目、重名负责人和事务回滚行为。

4. 完成 Linux/CI/CD 验证
   - 当前仅在 Windows 实际运行过 OCR；Linux 二进制已构建并校验格式/哈希，但未在 Linux 执行释放、权限设置和检视。
   - 建立主项目流水线：JDK 17 测试、Node 构建、Linux OCR 冒烟、JAR/镜像构建、数据库迁移和部署回滚。
   - 当前可执行 JAR 约 107.5 MiB，需确认制品仓库、镜像层缓存和上传限制。
   - 多实例部署前为凌晨调度和通知重试增加分布式锁，避免重复执行。

## P2：技术债清理

- [ ] 使用 Flyway 或 Liquibase 替代 `init.sql` 与 `SchemaMigrationService` 双轨迁移，避免结构漂移。
- [ ] 删除已由 OCR 替代的旧 AI Skill、Python 脚本执行器、旧接口和无入口页面代码，确认不再有双引擎路径。
- [ ] 清理 `project_token`、`use_default_token`、`review_days`、单值 `owner_name` 等遗留字段和 DTO。
- [ ] Gitee SSH 增加带口令私钥支持、主机指纹固定和私钥文件权限检查。
- [ ] 优化 Excel 导入：导入前仓库校验、错误工作簿下载、负责人别名匹配，避免更新项目时覆盖已有配置。
- [ ] 优化前端包体；当前 Element Plus 产物约 921 kB、图表产物约 465 kB，构建仍有大分块警告。
- [ ] 增加 Actuator 健康检查、结构化日志、任务耗时指标、OCR 进程指标和告警。

## 最终验收标准

- [ ] Windows 和 Linux 各完成一次无需预装 OCR 的真实检视。
- [ ] 手动四种模式和“昨天代码”模式均有成功、失败、无变更用例。
- [ ] 凌晨调度连续两天无重复、无漏跑，每个项目每天一条结果。
- [ ] 问题能够按 Git 提交人归属，负责人看全项目，普通用户无法读取他人任务报告。
- [ ] 招乎按人发送不同问题，链接携带用户 ID 且登录身份由后端可信校验。
- [ ] 密钥不在仓库、日志和数据库明文中出现，生产调试日志关闭。
- [ ] CI 流水线、数据库迁移、备份、回滚和运行手册均完成演练。
