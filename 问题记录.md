# 问题记录

更新时间：2026-07-16

状态说明：`待修复` 表示代码或配置仍有缺口；`待验证` 表示能力已实现但没有完成真实联合测试；`已验证` 表示当前环境存在可复核证据。

## 当前问题

| 编号 | 级别 | 模块 | 问题与影响 | 状态 | 处理建议 |
|---|---|---|---|---|---|
| ISSUE-020 | 严重 | 身份认证 | 用户身份完全由客户端 `X-User-Id` 决定，可直接冒充管理员。 | 待修复 | 接入内部统一认证，服务端解析身份和角色，禁止客户端声明管理员。 |
| ISSUE-021 | 严重 | 数据权限 | 普通用户的问题列表已过滤，但仍能查看全部任务，并能通过 `/api/review-report/detail-by-task` 读取他人完整报告。已用徐梓琅身份读取任务 49 报告复现。 | 待修复 | 为项目、任务、报告、导出及配置接口增加统一授权策略和测试。 |
| ISSUE-022 | 严重 | 密钥安全 | `application.yml` 含招乎默认 Client Secret；模型 Key、Gitee 私钥也未加密存储。 | 待修复 | 删除默认密钥并立即轮换，改用 CI Secret/环境变量/内部密钥服务。 |
| ISSUE-023 | 高 | 日志安全 | `code-review.ai.debug-log-enabled` 当前为 `true`，可能记录内部代码、Prompt 和模型响应。 | 待修复 | 生产默认关闭，敏感字段脱敏，并设置日志保留和访问权限。 |
| ISSUE-024 | 高 | 项目负责人 | 当前项目负责人关系列表为空，旧值 `xuzilang` 未映射为徐梓琅。通知无责任人匹配时可能无人接收。 | 待修复 | 在项目管理中补选负责人，并提供旧负责人数据迁移脚本。 |
| ISSUE-025 | 高 | 权限历史数据 | 旧引擎产生的 45 条历史问题没有责任人。 | 已修复 | 已物理清理历史问题和缓存报告，并将关联任务问题统计归零；新任务按提交人重新归属。 |
| ISSUE-026 | 高 | 定时任务 | 当前没有任何 `SCHEDULE` 任务记录，凌晨 1 点昨天代码流程未完成真实验收。 | 待验证 | 使用有昨日提交的测试仓库完成一次手动昨天模式和一次实际/模拟调度。 |
| ISSUE-027 | 高 | 调度配置 | 项目 `scheduleEnabled=0`，但调度器使用 `listEnabled()` 遍历所有启用项目，开关与实际行为不一致。 | 待修复 | 明确产品语义；若支持项目开关，调度查询必须包含 `schedule_enabled=1`。 |
| ISSUE-028 | 高 | 提交人归属 | 责任人通过 `git blame` 作者姓名/邮箱包含中文姓名或工号进行匹配，英文别名、共享邮箱、删除行会匹配失败。 | 待修复 | 增加 Git 身份别名表、未匹配清单和人工分配入口。 |
| ISSUE-029 | 高 | Linux 运行 | Linux amd64/arm64 OCR 已打包并通过 ELF/哈希校验，但只在 Windows 实际执行过。 | 待验证 | 在目标 Linux 镜像验证解压、`chmod`、模型调用、报告和进程回收。 |
| ISSUE-030 | 高 | 多实例部署 | 凌晨调度和通知重试使用本机 `@Scheduled`，多实例会并发触发。唯一索引只能覆盖部分重复场景。 | 待修复 | 使用 ShedLock/数据库锁并完善幂等键、冲突处理和监控。 |
| ISSUE-031 | 中 | 报告 | 报告曾显示遗留的“检视 N 天”和单值 `ownerName`。 | 已修复 | 新报告已按 `reviewMode` 展示分支、提交区间/时间窗、多人负责人、触发方式、模型调用和 Token。 |
| ISSUE-032 | 中 | 通知 | 投递日志筛选只识别 `TASK_SUCCESS/TASK_FAILED`，招乎新流程写入 `DAILY_REVIEW_SUCCESS/DAILY_REVIEW_FAILED`。 | 待修复 | 统一事件枚举、中文名称、筛选和重试策略。 |
| ISSUE-033 | 中 | 通知 | 招乎真实生产发送没有联合验证；测试发送和无收件人兜底也缺少完整审计。 | 待验证 | 使用专用测试机器人/测试用户验证鉴权、卡片、个性化链接、重试和限流。 |
| ISSUE-034 | 中 | 模型上下文 | 任务 51 出现大量 `prompt tokens exceed 80%` 警告，说明大文件仍可能被 OCR 跳过，任务成功不等于所有文件均被检视。 | 待修复 | 在平台展示被跳过文件明细；按 OCR 支持方式控制上下文、文件范围和 Token 预算。 |
| ISSUE-035 | 中 | 迁移机制 | `init.sql` 和 `SchemaMigrationService` 同时维护表结构，遗留字段仍被实体和报告使用。 | 待修复 | 迁移到 Flyway/Liquibase，清理 `review_days`、Token 和旧负责人字段。 |
| ISSUE-036 | 中 | 引擎清理 | OCR 已成为主引擎，但旧 DeepSeek/Skill/脚本执行相关类、DTO 和表仍存在，可能形成双路径维护。 | 待修复 | 建立引用清单，删除无入口功能并补迁移脚本。 |
| ISSUE-037 | 中 | CI/CD | 主项目没有流水线配置，只有 vendored OCR 自带工作流；尚无 Linux 构建、迁移、部署和回滚自动化。 | 待修复 | 建立主项目 CI 和镜像发布流程，固定 JDK 17、Node、Go 与 OCR 版本。 |
| ISSUE-038 | 低 | 前端包体 | 前端构建通过，但 Element Plus 分块约 921 kB，仍触发大分块警告。 | 待优化 | 继续按需引入样式和组件，按页面拆分图表与管理功能。 |

## 已验证记录

| 编号 | 模块 | 证据 | 状态 |
|---|---|---|---|
| CHECK-001 | 手动 OCR 检视 | 任务 52 使用 `COMMIT` 模式成功：1 个提交、13 个文件、87/87 次模型调用成功、总 Token 434,464。 | 已验证 |
| CHECK-002 | 历史数据清理 | 管理员问题接口返回 0 条；历史任务问题及四级严重度统计归零，旧缓存报告已删除。 | 已验证 |
| CHECK-003 | Gitee SSH | 系统配置接口返回私钥已配置并具有指纹，项目仓库为 SSH 地址。 | 已验证配置 |
| CHECK-004 | 模型配置 | 当前有一个已启用且 Key 已配置的 OpenAI 兼容模型；任务 52 证明其曾成功调用。 | 已验证 |
| CHECK-005 | 多平台 OCR 制品 | JAR 包含 Windows/Linux 的 amd64/arm64 四套压缩二进制；格式和 SHA-256 校验通过。 | 制品已验证，Linux运行待验证 |
| CHECK-006 | 自动化构建 | 2026-07-16 执行 `mvn test -q` 与 `npm run build` 均通过，共有 17 个后端测试类。 | 已验证 |

## 流程判定

| 流程 | 判定 | 说明 |
|---|---|---|
| 手动单提交检视 | 可运行 | 有真实成功任务和调用统计。 |
| 手动分支区间/工作区/全量扫描 | 代码已接入，未完成本轮联合验收 | 历史 RANGE 有记录，但最新 OCR 兼容改造后的四模式未逐项回归。 |
| 手动“昨天代码” | 待验证 | 代码、开关和通知参数已接入，尚无成功任务记录。 |
| 每天凌晨 1 点自动检视 | 未验收 | 当前无定时任务记录，且调度开关语义存在问题。 |
| 问题按提交人归属 | 部分可用 | 新问题会 blame 匹配，历史数据未回填且匹配规则有限。 |
| 按人员发送招乎通知 | 待验证 | 分组与链接代码已实现，未做真实端到端发送。 |
| Linux 免安装部署 | 待验证 | 二进制已内置，尚未在 Linux 运行。 |
| 内部生产使用 | 暂不满足 | 身份伪造、报告越权、明文密钥是上线阻断项。 |

## 说明

本次审计没有触发新的真实 OCR 任务或招乎消息，避免额外消耗模型 Token 和误发内部通知。结论来自现有任务数据、接口权限复现、配置状态、单元测试和构建结果。
