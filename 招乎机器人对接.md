本文档描述了调用招乎企业通讯 API 发送自定义卡片消息的核心逻辑，适用于需要集成到 Claude Code Hook 或其他自动化场景。

---

## 2. API 基础配置

### 2.1 基础信息

```python
API_HOST = "http://gatewayoazh.cmbchina.cn"
CLIENT_ID = "${ZHAOHU_CLIENT_ID}"
CLIENT_SECRET = "${ZHAOHU_CLIENT_SECRET}"
ROBOT_ID = "${ZHAOHU_ROBOT_ID}"
TO_ID = "FCD122540D98DF1D1D8D449F3CD132B6" // 用户id
```

### 2.2 Token 缓存配置

```python
TOKEN_CACHE_FILE = "zh_token_cache.json"  # 缓存文件名
TOKEN_EXPIRE_SECONDS = 86400              # token 有效期 24 小时
BUFFER_TIME = 300                         # 预留 5 分钟缓冲
```

---

## 3. 核心功能逻辑

### 3.1 获取 Access Token（OAuth2 Client Credentials）

**请求方式**: POST

**请求地址**: `{API_HOST}/auth-service/oauth/token`

**请求参数** (URL Query):
| 参数 | 值 |
|------|-----|
| grant_type | client_credentials |
| client_id | CLIENT_ID |
| client_secret | CLIENT_SECRET |

**响应处理**:
```python
resp = requests.post(url, params=params)
token = resp.json().get("access_token", "")
```

**Token 缓存逻辑**:
1. 读取本地 JSON 缓存文件
2. 检查当前时间是否在 `expires_at` 之前
3. 如有效直接返回缓存的 token
4. 否则发起新请求获取 token，并更新缓存

### 3.2 发送自定义卡片消息

**请求方式**: POST

**请求地址**: `{API_HOST}/robot-service/single-message/custom-card`

**请求头**:
```python
headers = {
    "Authorization": f"Bearer {token}",
    "Content-Type": "application/json"
}
```

**请求体 JSON 结构**:
```json
{
    "fromId": "ROBOT_ID",
    "toId": "TO_ID",
    "content": [
        {
            "type": "title",
            "content": "卡片标题",
            "style": 0
        },
        {
            "type": "mdContainer",
            "content": "Markdown 内容"
        }
    ],
    "summary": "摘要内容(可选)"
}
```

**content 数组说明**:
- `type`: 元素类型（title=标题, mdContainer=Markdown容器）
- `content`: 内容文本
- `style`: 标题样式（0=默认, 1=蓝色, 2=红色, 3=绿色, 4=橙色）

---
## 4. 输入处理逻辑

### 4.1 命令行参数解析

```
用法: python script.py "卡片标题" "Markdown内容" [摘要]
```

```python
if len(sys.argv) >= 3:
    card_title = sys.argv[1].strip()
    markdown_content = sys.argv[2].strip()
    summary = sys.argv[3] if len(sys.argv) > 3 else ""
```

### 4.2 Claude Code Hook stdin 解析

**输入 JSON 结构**:
```json
{
    "session_id": "会话ID",
    "cwd": "工作目录",
    "hook_event_name": "TaskCompleted|Notification|Stop|StopFailure",
    "task_subject": "任务主题",
    "task_description": "任务描述",
    "message": "消息内容",
    "title": "标题",
    "notification_type": "通知类型",
    "last_assistant_message": "最后回复",
    "error": "错误类型",
    "error_details": "错误详情"
}
```

**事件名称映射**:
| hook_event_name | 中文映射 | 建议 title_style |
|-----------------|----------|------------------|
| TaskCompleted | 任务完成 | 1 (蓝色) |
| Notification | 通知 | 4 (橙色) |
| Stop | 响应结束 | 3 (绿色) |
| StopFailure | 响应失败 | 2 (红色) |

**各事件内容构建**:
1. **TaskCompleted**: `任务: {task_subject} | 描述: {task_description}`
2. **Notification**: `类型: {notification_type} | 标题: {title} | 内容: {message}`
3. **Stop**: 截断处理最后回复
4. **StopFailure**: `错误: {error} | 详情: {error_details} | 回复: {last_message}`

### 4.3 文本处理函数

**文本截断** (`truncate_text`):
- 最大长度默认 24 字符
- 超过时保留首尾各一半，中间用 `...` 连接

**Markdown 清理** (`clean_text`):
- 移除 Markdown 格式符号（# * _ ~ ` > [ ] ( ) { } | -）
- 将链接 `[text](url)` 转换为纯文本 `text`
- 换行符替换为空格
- 多个空格合并为一个

---

## 5. 完整调用流程

```
1. 检查命令行参数
   ↓ 有
2. 解析参数 → 发送消息 → 结束
   
   ↓ 无
3. 检查 stdin 是否有数据
   ↓ 有
4. 解析 JSON → 根据事件类型构建内容 → 发送消息 → 结束
   
   ↓ 无
5. 显示用法提示
```

---

## 6. 错误处理要点

1. **编码处理**: Windows 环境下需处理 GBK/GB2312 编码
2. **多编码尝试**: stdin 数据尝试 utf-8 → gbk → gb2312 → latin-1
3. **异常捕获**: JSON 解析、文件读写、网络请求均需 try-except
4. **Token 失效**: 缓存过期时自动重新获取

---
## 4. 输入处理逻辑

### 4.1 命令行参数解析

```
用法: python script.py "卡片标题" "Markdown内容" [摘要]
```

```python
if len(sys.argv) >= 3:
    card_title = sys.argv[1].strip()
    markdown_content = sys.argv[2].strip()
    summary = sys.argv[3] if len(sys.argv) > 3 else ""
```

### 4.2 Claude Code Hook stdin 解析

**输入 JSON 结构**:
```json
{
    "session_id": "会话ID",
    "cwd": "工作目录",
    "hook_event_name": "TaskCompleted|Notification|Stop|StopFailure",
    "task_subject": "任务主题",
    "task_description": "任务描述",
    "message": "消息内容",
    "title": "标题",
    "notification_type": "通知类型",
    "last_assistant_message": "最后回复",
    "error": "错误类型",
    "error_details": "错误详情"
}
```

**事件名称映射**:
| hook_event_name | 中文映射 | 建议 title_style |
|-----------------|----------|------------------|
| TaskCompleted | 任务完成 | 1 (蓝色) |
| Notification | 通知 | 4 (橙色) |
| Stop | 响应结束 | 3 (绿色) |
| StopFailure | 响应失败 | 2 (红色) |

**各事件内容构建**:
1. **TaskCompleted**: `任务: {task_subject} | 描述: {task_description}`
2. **Notification**: `类型: {notification_type} | 标题: {title} | 内容: {message}`
3. **Stop**: 截断处理最后回复
4. **StopFailure**: `错误: {error} | 详情: {error_details} | 回复: {last_message}`

### 4.3 文本处理函数

**文本截断** (`truncate_text`):
- 最大长度默认 24 字符
- 超过时保留首尾各一半，中间用 `...` 连接

**Markdown 清理** (`clean_text`):
- 移除 Markdown 格式符号（# * _ ~ ` > [ ] ( ) { } | -）
- 将链接 `[text](url)` 转换为纯文本 `text`
- 换行符替换为空格
- 多个空格合并为一个

---

## 5. 完整调用流程

```
1. 检查命令行参数
   ↓ 有
2. 解析参数 → 发送消息 → 结束
   
   ↓ 无
3. 检查 stdin 是否有数据
   ↓ 有
4. 解析 JSON → 根据事件类型构建内容 → 发送消息 → 结束
   
   ↓ 无
5. 显示用法提示
```

---

## 6. 错误处理要点

1. **编码处理**: Windows 环境下需处理 GBK/GB2312 编码
2. **多编码尝试**: stdin 数据尝试 utf-8 → gbk → gb2312 → latin-1
3. **异常捕获**: JSON 解析、文件读写、网络请求均需 try-except
4. **Token 失效**: 缓存过期时自动重新获取

---
