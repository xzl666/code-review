# AI 驱动代码检视平台任务拆分

本文基于《总体设计方案.md》拆分，用于后续研发排期、分工和逐步交付。任务按“先底座、再配置、再引擎、再闭环、最后增强”的顺序组织，每个阶段都应具备可独立验证的交付物。

## 1. 拆分原则

- 后端遵循 DDD 四层架构：`interfaces`、`application`、`domain`、`infrastructure`。
- 所有接口均使用 POST，Controller 只负责参数接收、基础校验和响应转换。
- 优先完成核心业务闭环：项目配置 -> 规则配置 -> 手动检视 -> 问题落库 -> 结果查询。
- AI Skill 是核心主线，规则、Prompt、Function Calling、结构化问题输出都围绕 Skill 设计。
- 生产增强能力分阶段补齐，不阻塞 MVP 验证。

## 2. 里程碑总览

| 阶段 | 名称 | 目标 | 主要交付 |
|---|---|---|---|
| M0 | 工程底座 | 搭建可运行 Spring Boot 后端骨架 | 工程结构、公共能力、数据库连接 |
| M1 | 数据模型与持久化 | 完成核心表、实体、Mapper、Repository | SQL、DO、领域模型、仓储实现 |
| M2 | 项目与令牌管理 | 完成项目基础数据闭环 | 项目 CRUD、默认令牌、导入、仓库连接测试 |
| M3 | Skill 与规则管理 | 完成 AI 检视规则建模 | Skill、AI 规则、脚本规则、Schema 校验 |
| M4 | 检视任务核心链路 | 跑通一次完整代码检视 | 任务创建、Git diff、AI 调用、问题落库 |
| M5 | 通知与查询闭环 | 完成结果展示和通知 | 任务历史、问题详情、Webhook 通知 |
| M6 | 前端管理后台 | 提供可操作后台界面 | Vue 页面、接口联调、图表看板 |
| M7 | 生产增强 | 补齐稳定性、安全和扩展能力 | 分布式锁、限流、审计、权限、沙箱 |

## 3. M0 工程底座

### 3.1 后端工程初始化

- 创建 Spring Boot 2.7 + JDK 17 工程。
- 引入 MyBatis-Plus、MySQL Driver、Validation、Swagger/OpenAPI、Lombok。
- 建立基础包结构：
  - `interfaces`
  - `application`
  - `domain`
  - `infrastructure`
  - `common`
- 配置多环境配置文件：`application.yml`、`application-dev.yml`、`application-prod.yml`。

交付物：

- 可启动后端工程。
- 健康检查或测试接口可访问。
- 基础依赖可正常加载。

验收标准：

- 本地启动无报错。
- 能连接 MySQL。
- Swagger/OpenAPI 页面可查看接口文档。

### 3.2 公共能力建设

- 统一响应结构：`ApiResponse<T>`。
- 统一分页结构：`PageResponse<T>`。
- 全局异常处理：参数异常、业务异常、系统异常。
- 业务异常类：`BizException`。
- 通用枚举：启停状态、删除状态、项目类型、规则类型、严重等级、任务状态。
- 日志基础配置与敏感字段脱敏工具。

交付物：

- 公共响应、异常、枚举、工具类。

验收标准：

- Controller 返回格式统一。
- 参数错误和业务错误返回可读错误码与错误信息。

## 4. M1 数据模型与持久化

### 4.1 数据库表结构

创建以下核心表：

- `cr_project`
- `cr_review_rule`
- `cr_ai_skill`
- `cr_script_rule`
- `cr_review_task`
- `cr_review_issue`
- `cr_notify_config`
- `cr_notify_template`
- `cr_system_config`
- `cr_distributed_lock`
- `cr_operation_log`

交付物：

- 初始化 SQL。
- 索引定义。
- 测试数据脚本。

验收标准：

- SQL 可在 MySQL 5.7 执行。
- 表字段与总体设计保持一致。
- 固定包含 `create_time`、`update_time`、`deleted`。

### 4.2 持久化对象与仓储

- 为核心表创建 MyBatis-Plus Entity。
- 创建 Mapper。
- 创建领域 Repository 接口。
- 在 infrastructure 层实现 Repository。
- 建立 Entity 与领域模型转换器。

交付物：

- Entity、Mapper、Repository、Converter。

验收标准：

- 每个核心表具备基础增删改查能力。
- 领域层不直接依赖 MyBatis-Plus。

## 5. M2 项目与令牌管理

### 5.1 项目管理

- 项目新增、编辑、详情、分页、逻辑删除。
- 项目启用、停用。
- 项目类型支持 `FRONTEND`、`BACKEND`。
- 项目字段包含仓库地址、默认分支、责任人、最近检视天数、项目访问令牌。

涉及接口：

- `/api/project/create`
- `/api/project/update`
- `/api/project/delete`
- `/api/project/detail`
- `/api/project/page`
- `/api/project/enable`
- `/api/project/disable`

验收标准：

- 项目可完整 CRUD。
- 删除为逻辑删除。
- 分页支持项目名称、项目类型、状态过滤。

### 5.2 默认访问令牌

- 支持系统默认 Gitee 访问令牌配置。
- 默认令牌按业务要求明文存储。
- 支持每日手动更新入口。
- 预留账号密码自动获取令牌接口，不做实际实现。

交付物：

- 系统配置领域模型。
- 默认令牌查询与更新接口。

验收标准：

- 项目未配置单独令牌时，可读取默认令牌。
- 日志中不打印完整令牌。

### 5.3 Excel 导入项目

- 支持 Excel 上传导入。
- 按“项目名称 + 仓库地址”判断重复。
- 已存在则更新，不存在则新增。
- 返回成功数、失败数和失败原因。

涉及接口：

- `/api/project/import-excel`

验收标准：

- 导入正常数据成功。
- 缺少必填字段时返回明确失败原因。
- 重复数据按规则更新。

### 5.4 仓库连接测试

- 根据项目仓库地址、分支和令牌测试 Gitee 仓库访问。
- 支持项目单独令牌优先，默认令牌兜底。

涉及接口：

- `/api/project/test-repo-connection`

验收标准：

- 正确仓库返回成功。
- 错误令牌、错误地址、错误分支返回明确错误信息。

## 6. M3 Skill 与规则管理

### 6.1 AI Skill 管理

- Skill 新增、编辑、删除、详情、分页。
- Skill 启用、停用。
- Function name、Function description、JSON Schema、版本号管理。
- 查看 Skill 被哪些规则引用。

涉及接口：

- `/api/skill/create`
- `/api/skill/update`
- `/api/skill/delete`
- `/api/skill/detail`
- `/api/skill/page`
- `/api/skill/enable`
- `/api/skill/disable`

验收标准：

- Skill 可绑定到 AI 规则。
- 被启用规则引用的 Skill 删除时需要校验或拦截。

### 6.2 JSON Schema 校验

- 校验 JSON Schema 语法。
- 校验 Function Calling 所需参数结构。
- 返回校验成功或具体错误位置。

涉及接口：

- `/api/skill/validate-schema`

验收标准：

- 合法 Schema 返回成功。
- 非法 Schema 返回明确错误信息。

### 6.3 检视规则管理

- AI 规则新增、编辑、删除、详情、分页、启停。
- 脚本规则新增、编辑、删除、详情、分页、启停。
- AI 规则支持绑定 Skill。
- 规则支持项目类型、严重等级、规则类型、排序。
- Prompt 模板支持变量：`${projectName}`、`${projectType}`、`${diffContent}`、`${branch}`、`${reviewDays}`。

涉及接口：

- `/api/rule/create`
- `/api/rule/update`
- `/api/rule/delete`
- `/api/rule/detail`
- `/api/rule/page`
- `/api/rule/enable`
- `/api/rule/disable`
- `/api/script/create`
- `/api/script/update`
- `/api/script/delete`
- `/api/script/detail`
- `/api/script/page`

验收标准：

- AI 规则必须绑定启用状态的 Skill。
- 脚本规则必须配置脚本语言和超时时间。
- 禁用规则不参与检视。

### 6.4 AI 生成脚本入口

- 提供通过 AI 生成脚本规则的接口入口。
- 第一阶段可先返回生成结果草稿，不直接入库。
- 生成结果需包含脚本内容、语言、参数说明、预期输出结构。

涉及接口：

- `/api/rule/generate-script`

验收标准：

- 能基于用户描述生成脚本草稿。
- 生成结果不自动启用，必须人工确认。

## 7. M4 检视任务核心链路

### 7.1 任务创建与状态流转

- 手动发起检视。
- 创建任务编号。
- 初始状态为 `PENDING`。
- 异步执行后流转为 `RUNNING`、`SUCCESS`、`FAILED`、`CANCELED`。
- 支持失败任务重试。
- 支持取消待执行任务。

涉及接口：

- `/api/review-task/manual-start`
- `/api/review-task/retry`
- `/api/review-task/cancel`

验收标准：

- 发起检视后立即返回任务编号。
- 任务执行过程状态可查询。
- 失败任务记录错误原因。

### 7.2 Git 仓库与 diff 获取

- 实现本地仓库目录管理。
- 支持 clone、pull。
- 获取最近 N 天提交。
- 生成对应 diff。
- 统计提交数量和 diff 文件数量。
- 跳过二进制文件和可配置忽略文件。

交付物：

- `LocalRepositoryManager`
- `GiteeRepositoryClient`
- `GitDiffService`

验收标准：

- 能对真实 Gitee 仓库生成 diff。
- 令牌错误、仓库不存在、分支不存在均能失败可追踪。

### 7.3 diff 分片

- 按文件拆分 diff。
- 超大文件按 hunk 拆分。
- 单次 AI 请求控制最大字符数。
- 保留文件路径、起止行号、代码片段上下文。

验收标准：

- 大 diff 不会导致单次请求超过配置上限。
- 分片信息可用于问题定位。

### 7.4 AI Function Calling 适配

- 根据规则 Prompt、项目上下文、diff 分片组装请求。
- 使用 Skill JSON Schema 构建 Function Calling 定义。
- 调用 DeepSeek OpenAI 兼容接口。
- 解析 function call 结果。
- AI 调用次数、耗时、失败原因可记录。

交付物：

- `OpenAiCompatibleChatClient`
- `DeepSeekClient`
- `FunctionCallingRequestBuilder`
- `AiResponseParser`

验收标准：

- AI 返回结构化问题数组。
- 非法响应不会导致任务整体不可恢复，应记录错误并继续或失败。

### 7.5 脚本规则执行

- 根据启用脚本规则执行脚本。
- 支持 Shell、Python、Node 的执行入口。
- 传入 diff、项目、规则参数。
- 强制超时。
- 输出统一问题结构。

涉及接口：

- `/api/script/test-run`

验收标准：

- 脚本超时会被终止。
- 脚本输出可解析为问题。
- 测试运行能返回 stdout、stderr、退出码。

### 7.6 问题落库与任务统计

- 将 AI 和脚本问题统一落库到 `cr_review_issue`。
- 更新任务统计：问题总数、严重等级数量、AI 调用次数、耗时。
- 支持同一任务下多规则、多问题。

验收标准：

- 完成一次检视后可在任务详情看到统计。
- 问题列表能按任务、项目、严重等级、来源过滤。

## 8. M5 通知与查询闭环

### 8.1 任务与问题查询

- 任务分页、详情、统计。
- 问题分页、详情、统计。
- 问题忽略。
- 问题标记已修复。
- 问题导出。

涉及接口：

- `/api/review-task/page`
- `/api/review-task/detail`
- `/api/review-task/statistics`
- `/api/review-issue/page`
- `/api/review-issue/detail`
- `/api/review-issue/ignore`
- `/api/review-issue/mark-fixed`
- `/api/review-issue/statistics`
- `/api/review-issue/export`

验收标准：

- 可按项目、任务、严重等级、状态、来源查询问题。
- 问题状态变更可持久化。

### 8.2 Webhook 通知

- 通知配置新增、编辑、删除、分页。
- Webhook 测试发送。
- 任务成功通知。
- 任务失败通知。
- Secret 加密存储。
- 支持签名头。

涉及接口：

- `/api/notify-config/create`
- `/api/notify-config/update`
- `/api/notify-config/delete`
- `/api/notify-config/page`
- `/api/notify-config/test-send`

验收标准：

- 测试通知能发送到指定 Webhook。
- 任务完成后自动通知。
- 日志不打印 Secret 明文。

### 8.3 通知模板

- 模板新增、编辑、详情、分页。
- 模板预览。
- 支持变量渲染：项目名、任务号、问题数、严重等级分布、任务链接。

涉及接口：

- `/api/notify-template/create`
- `/api/notify-template/update`
- `/api/notify-template/detail`
- `/api/notify-template/page`
- `/api/notify-template/preview`

验收标准：

- 模板变量可正确替换。
- 缺失变量有兜底展示。

### 8.4 看板统计接口

- 首页概览。
- 问题趋势。
- 严重度分布。
- 项目问题排行。
- 规则命中排行。

涉及接口：

- `/api/dashboard/overview`
- `/api/dashboard/issue-trend`
- `/api/dashboard/severity-distribution`
- `/api/dashboard/project-ranking`
- `/api/dashboard/rule-hit-ranking`

验收标准：

- 统计数据与任务、问题表一致。
- 支持最近 7 天、30 天时间范围。

## 9. M6 前端管理后台

### 9.1 前端工程底座

- 初始化 Vue 3 + TypeScript + Vite。
- 引入 Element Plus 或 Naive UI。
- 引入 ECharts。
- 配置路由、菜单、请求封装、统一错误处理。

交付物：

- 可启动前端工程。
- 基础布局：顶部栏、侧边菜单、内容区。

验收标准：

- 本地启动成功。
- 能访问首页和各菜单页面。

### 9.2 页面任务

- 首页看板：概览指标、趋势图、严重度分布、排行、最近任务。
- 项目管理：列表、增改删、启停、导入、仓库测试、一键检视。
- 检视规则：AI 规则与脚本规则列表、编辑、启停、Prompt 编辑。
- AI Skill：Skill 列表、Function 定义、JSON Schema 编辑与校验、引用关系。
- 脚本规则：脚本编辑、测试运行、日志展示。
- 检视任务：任务列表、详情、状态、统计、重试、取消。
- 问题列表与详情：代码片段、问题说明、修改建议、忽略、标记修复。
- 通知配置：Webhook 配置、测试发送、模板编辑、模板预览。

验收标准：

- 页面功能覆盖后端核心 API。
- 表单校验与后端校验一致。
- 任务和问题页面能完整展示一次检视结果。

## 10. M7 生产增强

### 10.1 并发与调度增强

- 定时检视任务。
- MySQL 分布式锁防重复执行。
- 单项目并发控制。
- 全局 AI 并发控制。
- AI 单任务调用次数限制。

验收标准：

- 多实例场景下同一项目定时任务不会重复执行。
- AI 并发超过上限时排队或拒绝。

### 10.2 安全增强

- DeepSeek API Key 加密存储。
- Webhook Secret 加密存储。
- 敏感日志脱敏。
- 脚本执行目录隔离。
- 脚本输出大小限制。
- 审计日志记录关键操作。

验收标准：

- 数据库不保存 DeepSeek API Key 明文。
- 日志不出现完整令牌、API Key、Secret。
- 脚本不能越权访问非任务目录。

### 10.3 权限控制

- 预留用户体系。
- 角色：管理员、普通用户、只读用户。
- 对项目、规则、脚本、任务触发等操作做权限控制。

验收标准：

- 只读用户不能修改配置。
- 普通用户不能管理 Skill 和脚本规则。
- 管理员具备全部权限。

### 10.4 可观测性与运维

- 任务执行日志。
- AI 调用日志摘要。
- 脚本执行日志。
- 关键指标埋点：任务耗时、AI 耗时、失败率、问题数量。
- 配置化忽略文件、最大 diff 长度、超时时间。

验收标准：

- 失败任务可通过日志定位到 Git、AI、脚本、通知中的具体环节。
- 核心限制项均可通过配置调整。

## 11. 推荐开发顺序

1. M0 工程底座。
2. M1 数据模型与持久化。
3. M2 项目与令牌管理。
4. M3 Skill 与规则管理。
5. M4 先实现“手动检视 + Git diff + AI 规则 + 问题落库”的最小闭环。
6. M5 补任务查询、问题查询和通知。
7. M6 前端页面联调。
8. M7 生产增强。

## 12. MVP 范围建议

第一版建议只做以下能力，尽快验证主链路：

- 后端工程底座。
- 核心表结构和持久化。
- 项目管理。
- 默认令牌配置。
- AI Skill 管理。
- AI 规则管理。
- 手动发起检视。
- Git diff 获取。
- DeepSeek Function Calling。
- 问题落库。
- 任务详情和问题列表。
- 简单 Webhook 通知。

MVP 暂缓项：

- Excel 导入。
- AI 生成脚本。
- 脚本沙箱高级隔离。
- 权限体系。
- 审计日志。
- 多实例分布式锁。
- 完整前端图表看板。

## 13. 可并行分工建议

后端 A：

- M0、M1 工程底座和数据库持久化。
- 公共响应、异常、枚举、Repository 模式。

后端 B：

- M2 项目管理、默认令牌、仓库连接测试。
- M3 Skill、规则、脚本规则管理。

后端 C：

- M4 检视任务引擎。
- Git diff、AI 调用、脚本执行、问题落库。

后端 D：

- M5 通知、模板、查询统计、看板接口。
- M7 分布式锁、限流、安全增强。

前端：

- M6 前端工程、基础布局、项目页、规则页、Skill 页、任务页、问题页、通知页、看板页。

测试：

- 接口测试用例。
- 任务状态流转测试。
- Git 仓库连接测试。
- AI 响应解析测试。
- 通知发送测试。
- 端到端主链路测试。

## 14. 风险与前置确认

| 风险 | 影响 | 建议 |
|---|---|---|
| Gitee 令牌权限不足 | 无法 clone/pull/diff | 先用真实仓库验证连接测试 |
| AI Function Calling 返回不稳定 | 问题无法结构化落库 | 强制 JSON Schema 校验并保留 raw response |
| diff 过大 | AI 调用失败或成本过高 | M4 必须实现 diff 分片和长度限制 |
| 脚本执行安全风险 | 可能访问敏感资源 | MVP 可先禁用生产脚本执行或仅允许管理员 |
| 定时任务重复执行 | 多实例重复检视 | M7 引入 MySQL 分布式锁 |
| 前后端接口变动 | 联调成本增加 | 先冻结 API DTO，再进入前端批量开发 |

## 15. 每阶段完成定义

每个阶段完成时至少满足：

- 代码合并到主干或开发分支。
- 核心接口有基础测试或可通过 Swagger 手动验证。
- 数据库变更有 SQL 脚本。
- 关键异常路径有明确错误返回。
- 涉及敏感信息时日志完成脱敏。
- 文档同步更新接口、配置和注意事项。

